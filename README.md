# beanweirdo

Monorepo.

- [`frontend/`](frontend) — the coffee study journal, a React + Vite + TypeScript app implementing the Claude Design export (`frontend/design/`).
- [`backend/`](backend) — not started yet.

## Frontend

```
cd frontend
npm install
npm run dev
```
