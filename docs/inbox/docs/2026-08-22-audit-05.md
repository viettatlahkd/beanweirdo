# Rà tài liệu · lượt 05

ngày: 2026-08-22
file đã rà: bàn giao lane QA — `docs/inbox/qa/2026-08-21-qa-01.md` … `-qa-11.md`, đối chiếu với
`frontend/src/content/logic.ts`, `docs/SPEC.html` và `docs/spine/SO-BAN-GIAO.md`
commit đã rà: `4698b4c` (main)

> Mười một ghi chú QA viết rất chắc: mỗi mục có nhãn, có bằng chứng `file:dòng`, có bước tái
> hiện, và tự nêu luật nào bị đụng. Phần lớn những gì lượt này tìm được **không nằm trong các
> ghi chú** — nó nằm ở bản tóm tắt bàn giao và ở chỗ nối giữa lane QA với sổ bàn giao.
>
> Bốn tín hiệu ①②③④ mà bàn giao nêu: tôi đã xác minh cả bốn trên code, cả bốn **đúng**. Không
> nhắc lại chi tiết ở đây — chúng đã được ghi đủ trong qa-02, qa-03, qa-11.

## Phát hiện

- **[SAI]** Bản tóm tắt bàn giao ghi "Không có migration nào" — có một, và chính lane QA viết nó
  · tài liệu: bản tóm tắt bàn giao phiên 2026-08-21 → 22, mục "Dữ liệu đã đụng": "Không có migration nào."
  · code: `backend/supabase/migrations/0017_unclassified_lowercase.sql`, tạo ở commit `ff88e30` ("fix(hours): typing Vietnamese into a tag field, and the bucket in lower case") — một commit của lane QA
  · tài liệu đối chiếu: `docs/inbox/qa/2026-08-21-qa-05.md:55–61` ghi **đúng** — "**Có migration** — đây là điểm cần chú ý nhất của PR này", kèm cả cảnh báo trùng số hiệu với lane kiến trúc
  · độ chắc: đã xác minh
  · vì sao nặng: 0017 là migration **dữ liệu**, không phải cấu trúc. Ai đọc bản tóm tắt rồi kết luận "không có gì phải chạy trên DB" sẽ để nguyên các dòng cũ mang `Khác`. qa-05 đã lường đúng hậu quả: hai chuỗi khác nhau thành hai nhóm chip riêng, không mất dữ liệu nhưng thống kê tách đôi.

- **[SAI]** Bản tóm tắt viết giá trị nhóm chìm là `"Khác"` — giá trị thật là `'khác'`, viết thường
  · tài liệu: bản tóm tắt, mục "Một điểm cho người dựng báo cáo": 'giá trị "Khác" chỉ nằm trong `hour_logs.kind`/`.project`…'
  · code: `backend/lib/journal.ts:72` — `export const UNCLASSIFIED = 'khác'`; `backend/supabase/migrations/0017_unclassified_lowercase.sql` — `update hour_logs set kind = 'khác' where kind = 'Khác'`
  · tài liệu đối chiếu: `docs/inbox/qa/2026-08-21-qa-05.md:44–48` ghi đúng — "Nhóm chìm đổi tên từ `Khác` thành `khác`… Trước: `Khác`. Sau: `khác`."
  · độ chắc: đã xác minh
  · vì sao nặng: đây đúng là câu được viết ra để cứu người dựng báo cáo khỏi một truy vấn hụt — và nó mang chính chuỗi sai. `where kind = 'Khác'` trả về 0 dòng trên DB đã chạy 0017. Hai lỗi này cùng một gốc: bản tóm tắt chép lại qa-02 (viết trước `ff88e30`) mà không chép qa-05 (viết sau).

- **[SAI]** `docs/spine/SO-BAN-GIAO.md` tự nhận là nơi duy nhất tra nợ tài liệu, nhưng không có một dòng nào của lane QA
  · tài liệu: `docs/spine/SO-BAN-GIAO.md:1–7` — "Sổ này là **một nơi duy nhất** để tra: có gì đang nợ tài liệu, ai chịu trách nhiệm, đã xong chưa"; và `:17` — "Bảy luật dưới đây phát sinh từ code đã merge, chưa luật nào được ghi vào"
  · đối chiếu: §A có đúng 7 luật, cột "Từ PR" ghi #2 / #4 / #6 — toàn bộ là PR của lane xương sống. §C (chờ chủ site quyết) cũng không có mục nào của QA
  · code/tài liệu: lane QA đã merge 11 PR vào `main` và đề xuất khoảng 30 câu chữ luật, chạm 8 luật (08.01 · 08.02 · 08.04 · 08.05 · 08.06 · 08.07 · 14.01 · 14.02)
  · độ chắc: đã xác minh
  · hệ quả: câu "Bảy luật dưới đây phát sinh từ code đã merge" đang sai — số thật lớn hơn nhiều. Người giữ bút trên specs đọc sổ sẽ không thấy mâu thuẫn 08.05 và chỗ thiếu 08.07, dù cả hai đã nằm trong `main` từ hôm qua.

- **[SAI]** Neo `file:dòng` trong qa-02 và qa-03 phần lớn không còn trỏ đúng trên `main`
  · tài liệu: `docs/inbox/qa/2026-08-21-qa-02.md`, `-qa-03.md` — mục "Bằng chứng"
  · các neo tôi thử trên `main` @ `4698b4c`:

    | ghi chú nói | dòng đó hiện là gì | kết quả |
    |---|---|---|
    | `backend/api/hours.ts:137` | `async function handleRenameKind(...)` | **trúng** |
    | `backend/lib/journal.ts:72` | `export const UNCLASSIFIED = 'khác'` | **trúng** |
    | `frontend/src/components/TagDeleteReview.tsx:124` | `aria-label={\`Xoá tag ${target.name}\`}` | gần đúng |
    | `frontend/src/screens/Hours.tsx:252` ("chỗ quyết định mở bảng hay xoá thẳng") | phần chú thích của dải báo lỗi | **trượt** |
    | `frontend/src/screens/Hours.tsx:110` ("giờ đóng dấu là giờ bắt đầu") | thân hàm `addRow` | **trượt** |
    | `frontend/src/components/TagBar.tsx:146` (onDoubleClick) | `onClick={onToggle}` | **trượt** |
    | `frontend/src/content/hours.ts:32` ("Khác") | một dòng chú thích rỗng | **trượt** |
    | `frontend/src/lib/useSessionTimer.ts:30 / :89 / :136` | không khớp mô tả | **trượt** |
    | `frontend/src/screens/Hours.tsx:661` (công tắc) | dòng trống | **trượt** |

  · độ chắc: đã xác minh
  · nguyên nhân không phải cẩu thả: mỗi ghi chú tự khai "Cắt từ: origin/main @ 6ceab14" hoặc `d2be6b9`, và đánh số dòng theo nhánh của nó. Nhưng bản tóm tắt bàn giao nói "Tất cả đã nằm trong main" mà không nói số dòng tính theo nhánh cũ. Riêng `Hours.tsx` đã đổi 377 dòng sau `6ceab14`.
  · điều đáng giữ: neo bằng **tên hàm/hằng** (`handleRenameKind`, `UNCLASSIFIED`) sống sót; neo bằng **số dòng** thì không.

- **[THIẾU]** Bảng đối soát xoá tag đụng cả luật 08.03, chỗ này chưa ai nêu
  · tài liệu: `docs/inbox/qa/2026-08-21-qa-02.md` nêu 08.05 (mâu thuẫn) và 08.01 (mâu thuẫn một phần), không nhắc 08.03
  · luật: `frontend/src/content/logic.ts:106` — 08.03 "Ở mọi thao tác sửa: lưu ngay từng thay đổi, không có nút lưu toàn trang." Ví dụ kèm luật: "không dùng PUT cả trang"
  · code: `backend/api/hours.ts:239` `handleDeleteKind` nhận `{ moves: [{to, ids}], rest }` — người dùng tick qua nhiều bước trong bảng, rồi mọi thay đổi đi trong **một** lần ghi lúc bấm xoá. Đó đúng là hình dạng mà 08.03 loại trừ
  · độ chắc: đã xác minh
  · không phải để bắt lỗi bản sửa — nó được người dùng yêu cầu trực tiếp. Nhưng nếu 08.05 được viết lại để tự nêu ngoại lệ (đề xuất 5 của qa-02) mà bỏ quên 08.03 thì luật vẫn còn mâu thuẫn ngầm ở chỗ thứ hai.

- **[THIẾU]** Luật 01.06 mô tả nhóm Practice bằng hai màu; code dùng hai bảng màu quay vòng
  · tài liệu: `frontend/src/content/logic.ts:59` — "Riêng nhóm Practice: dùng xanh than với hồng mận, tách hẳn khỏi hệ màu module", ví dụ `#102F35 · #C25C7C`
  · code: `frontend/src/content/hours.ts:60–67` `PROJECT_PALETTE` có **6** màu (hai màu của luật là hai màu đầu), và `KIND_PALETTE` có **7**; màu gán theo vị trí trong danh sách nên tag mới tự có màu, quay vòng khi hết
  · độ chắc: đã xác minh
  · vì sao nêu bây giờ: cơ chế này có từ trước lane QA, nhưng lane QA vừa làm tag **tạo, sửa, xoá được** — nên số tag giờ do người dùng quyết, và việc màu quay vòng trở thành hành vi người dùng nhìn thấy chứ không còn là chi tiết nội bộ. Đây là phát hiện của lượt này, không phải của QA.

- **[THIẾU]** Không luật nào nói giờ được cộng thế nào — câu hỏi đang treo của QA là câu hỏi về luật, không chỉ về cách tính
  · tài liệu: bản tóm tắt bàn giao, mục cuối: hai hoạt động chồng khung giờ mà cùng tick done thì "giờ hữu ích" cộng cả hai
  · luật: nhóm 14 `[[hours]]` (`logic.ts:149–154`) có 4 luật, đều về bố cục và về sửa/xoá. Không luật nào nói một giờ đồng hồ có thể được đếm hai lần hay không
  · code: `frontend/src/lib/hoursStats.ts` — cộng thẳng `mins`, không xét chồng lấn
  · độ chắc: đã xác minh
  · qa-03 nêu đúng rằng "không có luật nào nói về việc đồng hồ có chạy tiếp khi rời màn" và đề xuất ba luật cho chuyện đó. Chỗ này là một khoảng trống **khác**, chưa ai nêu: không phải "đồng hồ chạy thế nào" mà "hai đồng hồ cùng chạy thì tổng ra bao nhiêu". Chạy nhiều đồng hồ song song vừa được bật hôm qua, nên khoảng trống này mới thành thật.

## Đã kiểm và thấy ĐÚNG (ghi để lượt sau khỏi kiểm lại)

- Bốn tín hiệu ①②③④ của bàn giao: đúng cả bốn.
  `UNDO_TTL_MS = 10 * 60 * 1000` (`frontend/src/lib/useUndoStack.ts`); nhánh xoá tag thẳng vs mở bảng;
  `at` nay là giờ bắt đầu; hai chỗ cần làm rõ ở 08.04 đúng như qa-11 mô tả.
- Ba endpoint mới có thật và đúng hợp đồng đã khai: `backend/api/hours.ts:409` (PATCH kinds),
  `:410` (DELETE kinds), `:416` (PATCH assign).
- Số hàm serverless vẫn **11 file** — ba đường mới là sub-resource trong `api/hours.ts`, không phải
  file mới. Trần 12 hàm của Vercel **không** bị đụng, và dòng tương ứng trong `docs/SPEC.html` vẫn đúng.
- Đánh số luật trong toàn bộ ghi chú QA (08.01 … 08.07, 14.01 … 14.04) khớp đúng thứ tự item hiện tại
  trong `logic.ts`. Nhóm 08 và 14 không bị PR `8f7b938` làm xê dịch — chỉ nhóm 09 đổi.
- `docs/SPEC.html`: bảng endpoint ghi `/api/hours` một dòng, không liệt kê sub-resource, nên
  ba đường mới **không** làm dòng đó sai. SPEC không mô tả cột của `hour_logs`, nên việc `at` đổi
  nghĩa không làm câu nào trong SPEC sai — nó chỉ là chỗ SPEC chưa bao giờ nói tới.

## Nhắc lại chưa xử lý

- lượt 01, cả bốn mục — chưa mục nào được đụng. PR #24 vẫn đang mở.
- lượt 03, chín mục của biên bản v4 — chưa mục nào đóng.
- lượt 03 + lượt 04, `docs/spine/SO-BAN-GIAO.md` B1 ("SPEC ghi 67 luật", thực tế 64) — vẫn còn.
- lượt 04, PR #25 — chưa merge lúc viết dòng này.

## Đề xuất câu chữ (không tự áp dụng)

Với bản tóm tắt bàn giao, nếu nó còn được dùng lại:

- "Không có migration nào" → "Một migration: `0017_unclassified_lowercase.sql` (đổi dữ liệu, không
  đổi cấu trúc). Xem qa-05."
- 'giá trị "Khác"' → "giá trị `khác` (viết thường, từ commit `ff88e30`)".
- Thêm một dòng: "Số dòng trong các ghi chú tính theo nhánh của từng PR, không theo `main`."

Với `docs/spine/SO-BAN-GIAO.md`:

- `:17` "Bảy luật dưới đây phát sinh từ code đã merge" → bỏ chữ "Bảy", hoặc thêm một mục §A phụ
  gom các đề xuất của lane QA. Nếu sổ giữ nguyên là sổ của riêng lane xương sống thì nên sửa câu
  mở đầu, vì hiện nó tự nhận là "một nơi duy nhất".

Với `frontend/src/content/logic.ts` — chỉ nêu chỗ cần đụng, câu chữ để người giữ bút viết:

- 08.05: nếu viết lại để tự nêu ngoại lệ, xét luôn 08.03 — bảng đối soát vi phạm cả hai.
- Nhóm 01: luật 01.06 nên nói rõ hai màu đó là đầu một bảng màu quay vòng, không phải toàn bộ
  bảng màu của nhóm Practice.
- Nhóm 14: cần một luật trả lời "hai hoạt động chồng giờ thì tổng ra bao nhiêu" — đây là câu chủ
  site phải quyết trước, không phải câu người viết luật tự trả lời.
