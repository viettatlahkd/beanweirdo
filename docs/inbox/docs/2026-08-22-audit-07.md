# Rà tài liệu · lượt 07

ngày: 2026-08-22
file đã rà: `docs/superpowers/` — 1 spec + 4 plan, đều đề ngày 2026-08-13
commit đã rà: `65bfe15` (main)

> Năm file này khác mọi thứ tôi đã rà: chúng là **kế hoạch**, không phải mô tả hiện trạng. Một
> kế hoạch cũ không sai chỉ vì thực tế đã đi khác — nó là bản ghi của một quyết định tại một
> thời điểm. Nên lượt này không đi đếm từng chỗ "plan nói X, code làm Y".
>
> Câu hỏi đúng là: **một người (hoặc một agent) mở thư mục này hôm nay sẽ hiểu nhầm điều gì?**
> Và câu trả lời nặng hơn tôi tưởng, vì bốn file plan không tự nhận là lịch sử — chúng tự nhận
> là việc cần làm, kèm hướng dẫn cách làm.

## Phát hiện

- **[SAI]** Bốn plan còn nguyên **107 ô việc chưa tick**, không ô nào được tick, dù toàn bộ đã làm xong từ lâu
  · tài liệu: `docs/superpowers/plans/` — đếm `- [ ]` từng file: admin-backend **32**, shared-renderer-live-data **34**, admin-frontend **21**, schema-migration **20**. Số ô `- [x]`: **0** ở cả bốn
  · tài liệu: mỗi file mở đầu bằng đúng một câu chỉ thị — "**For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development … to implement this plan **task-by-task**. Steps use checkbox (`- [ ]`) syntax for **tracking**."
  · code: việc đã xong. `backend/supabase/migrations/` có 17 file (Track A), `packages/post-renderer/` có đủ 5 renderer (Track B), `frontend/src/admin/` và `backend/api/` chạy thật (Track C)
  · độ chắc: đã xác minh
  · vì sao xếp [SAI] chứ không phải "tài liệu cũ": ô checkbox trong file này không phải trang trí — chính tài liệu khai nó là cơ chế theo dõi. 107 ô trống là một phát biểu, và phát biểu đó sai.

- **[SAI]** Hai plan được viết cho một kiến trúc chưa bao giờ tồn tại: app Next.js ở `admin/`
  · tài liệu: `docs/superpowers/specs/2026-08-13-post-authoring-admin-design.md` — sơ đồ kiến trúc: "`admin/` # MỚI — 1 app Next.js trên Vercel: FE (pages) + BE (API routes)". `plans/2026-08-13-admin-backend.md` và `-admin-frontend.md` viết trọn vẹn quanh nó
  · code: **không có thư mục `admin/`** ở gốc repo. Khu quản trị nằm ở `frontend/src/admin/` (React + Vite, không phải Next.js) và `backend/api/` (hàm serverless Vercel, không phải App Router)
  · số đo: trong 75 đường dẫn có thư mục mà năm file nhắc tới, **44 không tồn tại**, và **35** trong số đó nằm dưới `admin/` — `admin/app/api/posts/route.ts`, `admin/app/login/page.tsx`, `admin/lib/auth.ts`, `admin/package.json`…
  · độ chắc: đã xác minh
  · hệ quả cụ thể, không giả định: một agent làm theo chỉ thị ở đầu file sẽ tạo `admin/app/api/posts/route.ts` — trùng chức năng với `backend/api/posts/index.ts` đã có, ở một framework khác. Và `docs/SPEC.html` ghi rõ trần 12 hàm serverless của Vercel, hiện đang ở 11.

- **[SAI]** Bảng "Handoff to Track C-FE" mô tả sai cách xác thực đang chạy
  · tài liệu: `docs/superpowers/plans/2026-08-13-admin-backend.md:865` — "All routes require `Authorization: Bearer <supabase access_token>`"
  · code: `backend/lib/auth.ts:28` — token do backend tự phát và tự ký bằng `createHmac('sha256', …)`, không phải access token của Supabase. `/api/login` đổi mật khẩu tĩnh lấy vé
  · code đối chiếu: `backend/supabase/migrations/0008_single_author_journal.sql` nói thẳng điều này — "The app that actually got built never uses Supabase Auth… There is no row in auth.users"
  · độ chắc: đã xác minh
  · đây là chỗ dễ gây hại nhất trong bảng: hình dạng header (`Authorization: Bearer …`) giống hệt, chỉ nguồn token là khác. Đọc lướt sẽ không thấy.

- **[SAI]** Cùng bảng đó, hai dòng mô tả hợp đồng không còn tồn tại
  · tài liệu: `docs/superpowers/plans/2026-08-13-admin-backend.md:854–863`
  · `PATCH /api/posts/:id` — body ghi `{templateId,…}`. Cột `posts.template_id` đã bị **bỏ** ở `backend/supabase/migrations/0005_fixed_post_templates.sql`, thay bằng `posts.template` kiểu text
  · `POST /api/templates` với body `{name,layout,accent,onColor,tint,tint2}` — `backend/api/templates.ts` chỉ có **GET** và **PATCH**, không có POST; và bảng `templates` sau `0013`/`0014` mang cột `{name, description, renderer, body, sort_order}`, không còn layout/màu nào
  · độ chắc: đã xác minh
  · phần còn lại của bảng thì vẫn đúng ở mức đường dẫn: `/api/posts`, `/api/posts/:id`, `/api/posts/:id/status`, `/api/upload` đều tồn tại đúng như mô tả. Đáng ghi nhận — hợp đồng trên dây sống sót, chỉ có kiến trúc file là không.

- **[SAI]** Mục "Ngoài scope" của spec liệt kê ba việc mà nay đều đã làm hoặc đã đổi
  · tài liệu: `docs/superpowers/specs/2026-08-13-post-authoring-admin-design.md` — "Ngoài scope (không làm ở lần này)"
  · "Trang/luồng quản lý riêng cho `modules` … module vẫn sửa tay qua Table Editor" → code: `frontend/src/admin/lib/apiClient.ts:226,235,244` có `createModule`/`updateModule`/`deleteModule`; `backend/api/modules/` và tab Nội dung trong CMS dựng đủ
  · "Sửa nội dung phần personal journal (`hour_logs`, `notes`, `activity_kinds`) — không đổi gì ở đây" → code: lane QA đã merge 11 PR đúng vào đó, thêm ba đường trên `/api/hours` và migration `0017`
  · "Template system (layout + màu) tách khỏi `modules`, admin tạo/sửa được" (mục *trong* scope) → code: bảng `templates` bị bỏ ở `0005` rồi dựng lại ở `0013`/`0014` với ý nghĩa khác hẳn — nội dung lưu sẵn + renderer, không còn layout/màu — và vẫn **chưa** tạo được từ admin
  · độ chắc: đã xác minh

- **[SAI]** Hai plan trỏ vào một file wireframe không còn tồn tại, và bảo lấy giá trị CSS từ đó
  · tài liệu: `docs/superpowers/plans/2026-08-13-admin-frontend.md:5,17` — "approved via wireframe during brainstorming (`.superpowers/brainstorm/2212-1786598470/content/full-mockup.html`)" và "lift the CSS values from that file rather than re-deriving them"
  · code: **không có thư mục `.superpowers/`** trong repo
  · độ chắc: đã xác minh
  · đây là lần thứ hai cùng một kiểu hỏng: lượt 04 tìm ra `docs/doi-soat-tool/extract.js` đọc bản design từ một scratchpad của phiên đã đóng. Cả hai lần, một tài liệu trong repo lấy nguồn chân lý từ một chỗ ngoài repo, và chỗ đó biến mất. Lane xương sống đã sửa ca thứ nhất ở PR #27 bằng cách đưa công cụ vào `tools/design-audit.mjs`; ca này thì chưa ai đụng.

- **[MƠ HỒ]** Spec đề "Status: approved, ready for planning", không chỗ nào nói nó đã được thực hiện xong hay đã bị thay thế
  · tài liệu: `docs/superpowers/specs/2026-08-13-post-authoring-admin-design.md:4`
  · độ chắc: đã xác minh
  · "approved, ready for planning" đọc ra được hai nghĩa hoàn toàn khác nhau tuỳ người đọc có biết bối cảnh hay không: "đã duyệt, chờ ai đó lập kế hoạch" (sai, kế hoạch có rồi và đã chạy xong) hay "đã duyệt hồi tháng 8" (đúng). Không có ngày cập nhật, không có dòng "superseded by".

- **[THIẾU]** Năm file này nằm ngoài mọi cơ chế theo dõi của dự án
  · tài liệu: `docs/spine/SO-BAN-GIAO.md` — không mục nào nhắc `docs/superpowers/`; `docs/SPEC.html` cũng không
  · đối chiếu: `docs/SPEC.html` liệt kê `docs/spine/`, `backend/scripts/dev-server.mjs`, `tools/`… nhưng không nói `docs/superpowers/` là gì hay còn dùng không
  · độ chắc: đã xác minh
  · 3.290 dòng tài liệu không ai sở hữu, không ai đánh dấu là đã xong, và tự xưng là việc cần làm.

## Đã kiểm và thấy ĐÚNG

- Đường dẫn API trên dây phần lớn còn nguyên: `/api/posts`, `/api/posts/:id`, `/api/posts/:id/status`,
  `/api/upload` đều tồn tại và đúng method như bảng hợp đồng mô tả.
- Track A và Track B về cơ bản đã ra đúng thứ đã định: `packages/post-renderer` là workspace riêng,
  chứa component thuần trình bày nhận `(template, post)`, có render-prop override cho admin — đúng
  như plan Track B mô tả, và `frontend/src/lib/postToRenderer.ts` nay là bộ chuyển đổi duy nhất
  dùng chung hai đường.
- Vòng đời trạng thái bài (draft · published · archived · deleted + previous_status) ra đúng như
  spec thiết kế; `backend/lib/posts.ts` khớp.
- Ba commit sửa trong lịch sử (`ba7af15`, `f0b3797`, `0c361d4`) cho thấy các plan này từng được
  soát lại nghiêm túc trước khi chạy — vấn đề là sau khi chạy xong thì không ai đóng sổ.

## Nhắc lại chưa xử lý

- lượt 05 + lượt 06, `docs/spine/SO-BAN-GIAO.md:17` vẫn ghi "**Bảy** luật", §A vẫn 7 dòng, vẫn
  không dòng nào của lane QA.
- lượt 06, ba chỗ `docs/SPEC.html` tự mâu thuẫn (số luật · đường ghi `notes` · module trên Trang chủ).
- lượt 04, `docs/SO-TUNG-ELEMENT.html` và `docs/DOI-SOAT-DESIGN-V4.html` vẫn chưa commit — nay đã
  có bản thay thế trong repo (`docs/spine/DOI-SOAT-DESIGN.md`, `tools/design-audit.mjs` từ PR #27),
  nên câu hỏi chuyển thành giữ làm lưu trữ hay xoá.

## Đề xuất câu chữ (không tự áp dụng)

Chỗ này tôi đề xuất **cách xử lý thư mục**, không chỉ câu chữ — vì sửa từng câu trong 3.290 dòng
kế hoạch đã chạy xong là công vô ích.

- Đơn giản nhất: thêm một dòng đầu mỗi file — "**Đã thực hiện xong tháng 8/2026. Giữ lại làm bản
  ghi quyết định. Kiến trúc thực tế khác bản kế hoạch: khu quản trị nằm ở `frontend/src/admin/` +
  `backend/api/`, không phải app Next.js ở `admin/`. Đừng làm theo file này.**"
- Gỡ hoặc sửa câu chỉ thị "REQUIRED SUB-SKILL … implement this plan task-by-task" ở đầu bốn plan —
  đây là câu duy nhất khiến tài liệu lịch sử trở thành tài liệu nguy hiểm.
- Hoặc: đổi `- [ ]` thành `- [x]` cho những việc đã xong. Trung thực hơn, nhưng tốn công và vẫn để
  lại các task mô tả kiến trúc `admin/` không bao giờ đúng.
- Thêm một dòng vào `docs/spine/SO-BAN-GIAO.md` §D nói `docs/superpowers/` là gì và số phận của nó —
  để nó thôi nằm ngoài sổ.
- Nếu quyết định giữ: đưa `full-mockup.html` vào repo, hoặc bỏ hai câu trỏ tới nó. Một tài liệu
  không nên lấy chân lý từ chỗ nằm ngoài repo — bài học đã trả giá một lần ở lượt 04.
