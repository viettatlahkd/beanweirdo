# Rà tài liệu · lượt 06

ngày: 2026-08-22
file đã rà: `docs/SPEC.html` — **bản viết lại** (PR #24, `224f85f`), lần rà thứ hai
commit đã rà: `8aa1717` (main)

> Tin tốt trước: `docs/SPEC.html` **nay đã nằm trong git** — chỗ tôi kêu ở lượt 01. Và cả bốn
> phát hiện của lượt 01 đều đã được xử lý: 16→17 màn hình, 14→17 migration, hai endpoint đổi
> "ghi"→"Có", con số test viết lại đúng. `docs/spine/SO-BAN-GIAO.md` §B nay XONG cả tám mục,
> B1 cũng đã sửa lại đúng ("67 (tôi ghi sai) → 64 (đúng lúc đó) → 68 hôm nay").
>
> Cái lượt này tìm được gần như đều cùng một hình dạng: **ô số liệu và phụ lục được cập nhật,
> đoạn văn trong thân bài thì không**. Ba chỗ tài liệu nay tự mâu thuẫn với chính nó.

## Phát hiện

- **[SAI]** SPEC nói hai con số luật khác nhau, cách nhau 150 dòng
  · tài liệu: `docs/SPEC.html` — ô số liệu đầu trang ghi "**68** quy tắc hệ thống"; thân mục *Bộ quy tắc* mở đầu bằng "**Sáu mươi tư** quy tắc, chia làm 14 nhóm trong 3 phần"
  · code: `frontend/src/content/logic.ts` — đếm bằng cách import `LOGIC` và cộng `items.length`: **68** luật, 14 nhóm, 3 phần. Ô số liệu đúng, câu văn sai
  · độ chắc: đã xác minh
  · bảng *Đã đổi kể từ bản đầu* của chính tài liệu cũng ghi "Bộ quy tắc 64 → 68 luật" — nên trong một trang có ba chỗ nói về con số này, hai đúng một sai

- **[SAI]** SPEC nói cả hai điều trái ngược về đường ghi vào bảng `notes`
  · tài liệu: bảng *Cơ sở dữ liệu* ghi "`notes` — Bảng ghi chú cũ — hiện rỗng, **nhưng đường ghi vẫn còn qua `/api/notes`**"; mục *Khoảng trống đã biết* vẫn ghi "Bảng `notes` hiện rỗng và **không còn đường ghi vào** — nội dung Ghi 01 đã chuyển hết sang bài viết"
  · code: `backend/api/notes.ts` phục vụ POST/PATCH/DELETE; `frontend/src/admin/lib/apiClient.ts` gọi cả ba; `frontend/src/admin/components/PostsPanel.tsx` dựng UI sửa note. Câu ở bảng DB đúng, câu ở Khoảng trống sai
  · độ chắc: đã xác minh
  · đây là lượt 01 mục 4 được sửa **một nửa**: chỗ tôi trích dẫn thì được sửa, chỗ còn lại nói cùng một điều thì không. Câu "nội dung Ghi 01 đã chuyển hết sang bài viết" cũng vẫn sai — `frontend/src/screens/Notes.tsx` đọc song song `useNotes()` và `usePublishedPosts()`

- **[SAI]** SPEC nói cả hai điều trái ngược về việc module có luôn hiện trên Trang chủ hay không
  · tài liệu: mục *Mô hình nội dung* — "Quy tắc đã chốt: một module đã tạo thì luôn hiện ở sidebar **và trang chủ**, kể cả khi chưa có bài nào". Mục *Khoảng trống đã biết* — "module đặc biệt công khai như Ghi 01 vẫn hiện ở Mục lục và sidebar, **chỉ vắng ở Trang chủ**". Bảng *Đã đổi kể từ bản đầu* — "Trang chủ chỉ trưng module thường"
  · code: `frontend/src/data/useModules.tsx` — `landingModules` lọc `kind !== 'special' && visibility !== 'private'`; dùng ở `frontend/src/screens/Landing.tsx`. Hai chỗ sau đúng, đoạn *Mô hình nội dung* sai
  · độ chắc: đã xác minh
  · liên quan lượt 02: luật 05 trong `logic.ts` vẫn giữ nguyên câu cũ ("luôn hiện ở sidebar và [[landing]]"), và sổ bàn giao §A đã nhận việc ở A3/A4. Đoạn SPEC này là chỗ thứ ba nói cùng một điều đã lỗi thời — không nhắc lại phần luật, chỉ nêu chỗ SPEC

- **[SAI]** "trừ một bài backend bị cố ý bỏ qua" — hôm nay không bài nào bị bỏ qua, và con số đó phụ thuộc máy
  · tài liệu: `docs/SPEC.html` — mục *Môi trường & triển khai*: "262 bài cho giao diện, 137 bài cho backend — tất cả đang qua, trừ **một** bài backend bị cố ý bỏ qua"
  · code: `backend/api/integration.test.ts` — **hai** test tự bỏ qua bằng `ctx.skip()`, mỗi cái một điều kiện khác nhau: một cái khi thiếu `ADMIN_PASSWORD`/`ADMIN_SESSION_SECRET` (`:45–48`), một cái khi không gọi được Supabase (`isSupabaseReachable()`, `:26–40`)
  · đo thật trên `8aa1717`: `npm run test:api` → **137 passed (137)**, không có skip nào. Cũng chính máy này, sáng nay khi Supabase chưa chạy: 136 passed | 1 skipped
  · độ chắc: đã xác minh
  · hai chỗ sai: "một" đáng ra là "hai", và "cố ý bỏ qua" mô tả sai bản chất — test không bị tắt, nó tự bỏ qua khi không với tới được cơ sở dữ liệu thật. Người tiếp quản chạy thử mà thấy 137/137 sẽ tưởng tài liệu sai; chạy trên máy chưa dựng Supabase mà thấy 2 skip cũng tưởng tài liệu sai

- **[SAI]** "Ba luật gần đây nhất được thêm vào" liệt kê ba luật không còn là gần đây nhất
  · tài liệu: mục *Bộ quy tắc* — ba ví dụ: tiêu đề khai báo một chỗ · mô tả lấy sapo · module đã tạo luôn hiện
  · code: bốn luật thêm gần nhất nằm ở nhóm 09, từ PR #23 (`8f7b938`) — nhóm 09 đi từ 6 lên 10 luật, gồm luật phổ quát cho template, luật về đoạn cuối đường dẫn, luật không đổi hoa/thường tiêu đề, và luật ba nơi phải sửa khi thêm template
  · độ chắc: đã xác minh
  · ví dụ thứ ba trong danh sách ("Module đã tạo luôn hiện") lại đúng là luật đang bị `visibility` làm cho lỗi thời — nên đoạn này vừa cũ vừa trỏ vào chỗ sai

- **[MƠ HỒ]** Tiêu đề một mục trong *Khoảng trống đã biết* nói ngược với thân của chính nó
  · tài liệu: mục *Khoảng trống đã biết* — tiêu đề "**Module chưa có cách ẩn**", thân "**Đã có cột `visibility`** (migration 0015)…"
  · độ chắc: đã xác minh
  · thân đã được cập nhật, tiêu đề thì chưa. Người quét tiêu đề để lên kế hoạch sẽ ghi nhầm việc này vào danh sách còn nợ

- **[THIẾU]** Bản SPEC đề ngày 22.08 không ghi một dòng nào về việc lane QA đã merge
  · tài liệu: `docs/SPEC.html` — tìm cả trang: không có `resource=kinds`, không có `assign`, không có "10 phút", không có "giờ bắt đầu", không có "hoàn tác". Bảng *Đã đổi kể từ bản đầu* liệt kê 8 thay đổi, **toàn bộ** là của lane xương sống
  · code, những thứ đã nằm trong `main` và SPEC chưa nhắc:
    · nhóm chìm `'khác'` — chuỗi nằm trong `hour_logs.kind`/`.project`, **không có dòng tương ứng** trong `activity_kinds` (`backend/lib/journal.ts` `UNCLASSIFIED`, migration `0017`)
    · ba đường mới trên `/api/hours`: `PATCH ?resource=kinds`, `DELETE ?resource=kinds`, `PATCH ?resource=assign` (`backend/api/hours.ts`, `handleRenameKind` · `handleDeleteKind` · `handleAssign`)
    · `hour_logs.at` đổi nghĩa: nay là giờ bắt đầu phiên, trước là giờ bấm xong
    · bộ nhớ đệm hoàn tác có thêm hạn 10 phút (`frontend/src/lib/useUndoStack.ts`, `UNDO_TTL_MS`)
  · độ chắc: đã xác minh
  · đây đúng là hệ quả mà lượt 05 đã nêu: nợ tài liệu của lane QA không bao giờ tới được sổ bàn giao, nên vòng cập nhật SPEC này không nhìn thấy nó. Nhóm chìm `khác` là chỗ đắt nhất — SPEC là thứ người mới đọc, và bảng *Cơ sở dữ liệu* của nó dạy người ta đi từ `activity_kinds`, đúng cái đường sẽ bỏ sót nhóm này

## Đã kiểm và thấy ĐÚNG

- Mọi ô số liệu trừ một: 7 bảng · **17** migration · **11** API endpoint · **17** màn hình (11 + 6) ·
  5 kiểu trình bày · **399** test. Đo lại từng cái trên `8aa1717`, khớp. Riêng "68 quy tắc" thì ô
  số liệu đúng còn thân bài sai — xem phát hiện 1.
- 262 test giao diện và 137 test backend: chạy thật, đúng con số.
- `modules` 21 cột và `posts` 21 cột: khớp khi cộng dồn 17 migration.
- Bảng khu vực và danh sách màn hình cho phép: khớp `frontend/src/lib/area.ts`.
- Câu mới "cùng một bộ chuyển đổi dữ liệu cho cả hai đường… có một kiểm tra tự động bắt buộc hai
  đường cho ra kết quả giống hệt": đúng. `frontend/src/lib/postToRenderer.ts` là bản duy nhất,
  dùng chung bởi `screens/Article.tsx` (công khai) và `admin/screens/Preview.tsx` + `Editor.tsx`;
  `frontend/src/lib/postToRenderer.test.ts` có bộ test "one shape, one adapter".
- Trần 12 hàm serverless không bị đụng: vẫn 11 file trong `backend/api/`.

## Nhắc lại chưa xử lý

- **lượt 05, sổ bàn giao §A** — `docs/spine/SO-BAN-GIAO.md:17` vẫn ghi "**Bảy** luật dưới đây phát
  sinh từ code đã merge", §A vẫn chỉ có 7 dòng, vẫn không dòng nào của lane QA. PR #26 merge sau
  PR #24 nên vòng này chưa thể thấy; nêu lại để vòng sau không bỏ sót.
- lượt 03, chín mục của biên bản đối soát v4 — chưa mục nào đóng.
- lượt 04, `docs/SO-TUNG-ELEMENT.html` và `docs/DOI-SOAT-DESIGN-V4.html` — **vẫn chưa được commit**.
  `SPEC.html` đã vào git, hai file kia thì chưa; nay chúng là hai tài liệu duy nhất trong `docs/`
  nằm ngoài lịch sử.

## Đề xuất câu chữ (không tự áp dụng)

- Mục *Bộ quy tắc*, câu mở: "Sáu mươi tư quy tắc" → "Sáu mươi tám quy tắc".
- Mục *Khoảng trống đã biết*, mục "Bảng ghi chú cũ còn nằm đó": bỏ vế "không còn đường ghi vào",
  cho khớp với dòng đã sửa ở bảng *Cơ sở dữ liệu*.
- Mục *Mô hình nội dung*: "luôn hiện ở sidebar và trang chủ" → "luôn hiện ở sidebar và Mục lục;
  Trang chủ chỉ trưng module thường".
- Mục *Môi trường & triển khai*: "trừ một bài backend bị cố ý bỏ qua" → "hai bài kiểm tra tích hợp
  tự bỏ qua khi máy chưa dựng Supabase cục bộ; dựng đủ thì cả 137 bài đều chạy".
- Mục *Bộ quy tắc*: thay ba ví dụ cũ bằng bốn luật nhóm 09 thêm ở PR #23, hoặc bỏ hẳn đoạn ví dụ
  vì nó phải sửa mỗi lần bộ luật đổi.
- *Khoảng trống đã biết*: tiêu đề "Module chưa có cách ẩn" → "Cách ẩn module đã có, chưa có giao
  diện" (nếu đúng vậy) hoặc bỏ mục.
- Thêm một mục vào *Mô hình nội dung* hoặc *Cơ sở dữ liệu* về nhóm chìm `khác`: nó là chuỗi trong
  `hour_logs`, không phải dòng trong `activity_kinds`, nên truy vấn đi từ `activity_kinds` sẽ bỏ sót.
