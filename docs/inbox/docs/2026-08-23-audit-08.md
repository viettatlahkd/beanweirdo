# Rà tài liệu · lượt 08

ngày: 2026-08-23
file đã rà: `docs/inbox/qa/2026-08-23-qa-28-ban-giao-docs.md` (PR #58, chưa merge), đối chiếu với
`frontend/src/content/logic.ts`, `docs/SPEC.html` và code
commit đã rà: `e3338db` (main)

> Bản gộp này làm đúng việc nó nhận: bảng "luật đã chết ↔ luật thay thế" đứng trước là quyết
> định đúng, và mười dòng trong đó tôi kiểm được năm dòng trên code — đều khớp. Đọc tuần tự
> mười lăm ghi chú thì tôi đã chép nhầm heatmap ít nhất hai lần.
>
> Nên lượt này không đi kiểm lại 39 luật một cách máy móc. Tôi kiểm mẫu những luật **chở nặng
> nhất** (con số cụ thể, ngữ nghĩa dữ liệu), rồi tập trung vào ba câu hỏi được giao.

## Phát hiện

- **[SAI]** Luật đã chết "heatmap 26 tuần" vẫn còn sống nguyên văn trong code, ngay phía trên hằng số đúng
  · tài liệu: `qa-28`, bảng luật đã chết, dòng 1 — "Heatmap 26 tuần *(qa-13)* → **15 tuần** *(qa-23)*"
  · code: `frontend/src/lib/hoursStats.ts:493` — một khối `/** */` mồ côi: "An Anki-style grid: one square per day, **twenty-six weeks** of them." Ngay dưới nó, `:501` là khối thứ hai "**Fifteen weeks**", rồi `:509` `export const HEAT_WEEKS = 15`
  · độ chắc: đã xác minh
  · hai khối chú thích dán liền nhau, khối đầu không còn thuộc về khai báo nào. Đây đúng là loại chỗ tôi được giao đi tìm: người đọc code để tra sự thật sẽ gặp con số chết trước con số sống. Không thuộc lane tài liệu để sửa, nhưng thuộc phần phải báo.

- **[SAI]** "Mười hai nhánh" đếm thiếu — có ít nhất mười bốn
  · tài liệu: `qa-28`, mục *Hai chỗ trong tài liệu hiện đang sai* — liệt kê 12: đổi tên · tag project · tag task · giờ bắt đầu · giờ kết thúc · thời lượng · ghi chú · tick · thêm một lần · nhân đôi · gộp · xoá
  · code: `frontend/src/components/ActivityRow.tsx:389–412` — ngoài 12 nhánh trên còn `onPatchSitting(id, patch)` (**sửa một lần cụ thể**) và `onRemoveSitting(id)` (**xoá một lần**, khác với xoá cả hoạt động ở luật 24). Cả hai được nối thật, `:987`
  · độ chắc: đã xác minh
  · nêu ra không phải để bắt bẻ con số, mà vì nếu chép thẳng "mười hai nhánh" vào bộ luật thì luật sai ngay ngày nó vào. Xem thêm khuyến nghị câu hỏi 2 — tôi cho rằng đừng đếm nhánh trong luật.

- **[SAI]** `docs/SPEC.html` lệch lại ở hai ô số liệu, sau vòng cập nhật 22.08
  · tài liệu: `docs/SPEC.html` — ô số liệu ghi "**17** migration" và "**75** quy tắc hệ thống"
  · code: `backend/supabase/migrations/` có **20 file**; `frontend/src/content/logic.ts` đếm được **82 luật** trong **15 nhóm** (import `LOGIC`, cộng `items.length`)
  · độ chắc: đã xác minh
  · lượt 06 đã ghi nhận vòng trước sửa đúng cả hai ô này. Chúng lệch lại sau một ngày. Đây là loại lệch không sửa dứt được bằng cách sửa câu chữ — xem mục cuối.

- **[SAI]** Va chạm số hiệu migration mà `qa-05` cảnh báo từ 21.08 đã xảy ra thật, và vẫn còn
  · tài liệu: `docs/inbox/qa/2026-08-21-qa-05.md` — "Số hiệu 0017 vì lane kiến trúc vừa dùng 0015 và 0016 — nếu họ cũng đang viết 0017 thì hai file trùng số, cần đổi tên một trong hai trước khi chạy". `qa-28` mục *Còn nợ* nhắc lại
  · code: `backend/supabase/migrations/` có **cả hai** `0017_pinned_and_optional_order.sql` và `0017_unclassified_lowercase.sql`
  · độ chắc: đã xác minh
  · hệ quả cho tài liệu: `docs/SPEC.html` mô tả quy ước "viết file số mới, không bao giờ sửa file đã gửi đi chạy" — quy ước đó ngầm giả định số hiệu là duy nhất, và nay không còn. Thứ tự chạy giữa hai file 0017 không xác định được từ tên.

- **[THIẾU]** Ngữ nghĩa `hour_logs.mins` của hàng cha không có chỗ nào trong specs ghi lại
  · tài liệu: `qa-28`, mục *Schema đã đổi* — "số phút nằm ở các lần, hàng cha không giữ phút nào của riêng nó. Cột `mins` của hàng cha vẫn là giá trị cũ từ trước khi nó thành hàng cha, và không có gì giữ nó cho khớp — **cố ý**"
  · code: `backend/supabase/migrations/0019_hour_log_parent.sql`; `backend/lib/journal.ts:28,42` mang `parent_id`/`parentId`
  · tài liệu đối chiếu: `docs/SPEC.html` không mô tả cột của `hour_logs`; `logic.ts` nhóm 14 không nhắc
  · độ chắc: đã xác minh
  · đây là cái bẫy đắt nhất trong đợt này, cùng họ với `khác` ở lượt 05: một cột mang giá trị **không đúng** một cách có chủ ý. Ai cộng `sum(mins)` trên `hour_logs` sẽ đếm hai lần. Luật 23 của `qa-28` nói đúng điều đó ở tầng giao diện, nhưng tầng dữ liệu thì chưa tài liệu nào nói.

## Đã kiểm mẫu và thấy ĐÚNG

- Luật 35 — heatmap **15 tuần**: `frontend/src/lib/hoursStats.ts:509` `HEAT_WEEKS = 15`, neo vào tuần
  của ngày mở sổ (`:533` `openingWeek = weekStart(new Date(KICKOFF))`).
- Luật 1 — ngày mở sổ **20/08/2026**: `frontend/src/content/hours.ts:139` `KICKOFF = '2026-08-20'`.
- Luật 33 — khoảng chồng nhau chỉ tính một lần: `frontend/src/lib/hoursStats.ts:335,338` — "counting
  overlapping activities once… Intervals are merged before measuring".
  **Chỗ này đóng một câu hỏi treo**: lượt 05 ghi nhận lane QA để ngỏ "hai hoạt động chồng khung giờ
  cùng tick done thì cộng cả hai". Nay đã có câu trả lời và code làm đúng nó.
- Luật 20–23 — tên và tag ở hoạt động, giờ/thời lượng/trạng thái ở từng lần: khớp `parent_id` và
  cách `journal.ts` phơi `parentId`.
- `qa-28` nói đúng cả ba chỗ nó tự khai là sai: nhóm 14 đúng là **4 luật**; luật thứ hai của nhóm
  đúng là "sửa hoạt động chỉ hai nhánh"; `docs/SPEC.html` nhắc "Ghi 02" **đúng một lần**, ở mục
  *Mô hình nội dung*, chỉ để nói nó là module đặc biệt.

## Ba câu hỏi được giao — quyết định của tôi

Tôi quyết, người giữ bút áp dụng. Tôi không sửa `logic.ts` hay `SPEC.html`.

### 1. Tách nhóm 14 — **tách, thành bốn nhóm, đánh số từ 16**

Không để phình. Nhóm lớn nhất hiện nay là 05 với 11 luật; 39 luật trong một nhóm sẽ gấp ba rưỡi
cái lớn nhất và biến mọi dẫn chiếu "nhóm 14" thành vô nghĩa.

Dùng luôn năm cụm mà `qa-28` đã gom, gộp cụm đồng hồ (3 luật) vào cụm một-hoạt-động:

| nhóm | tên đề xuất | luật `qa-28` | số luật |
|---|---|---|---|
| 14 | `[[hours]]` — ngày và danh sách ngày | 1–9 | 9 |
| 16 | `[[hours]]` — một hoạt động | 10–19, 29–31 | 13 |
| 17 | `[[hours]]` — hoạt động nhiều lần | 20–28 | 9 |
| 18 | `[[hours]]` — bảng thống kê | 32–39 | 8 |

Giữ 14 cho cụm ngày để dẫn chiếu cũ không gãy hoàn toàn.

**Cảnh báo về số hiệu, cần biết trước khi đặt tên nhóm:** nhóm **15 đã tồn tại** — "Ảnh và ô màu",
và nó nằm trong phần B **giữa nhóm 07 và nhóm 08** theo thứ tự mảng. Tức thứ tự đọc đã không còn
trùng thứ tự số. Nhóm mới phải lấy từ 16, và nếu định xếp bốn nhóm `[[hours]]` cạnh nhau trong
mảng thì thứ tự số sẽ càng lệch. Đề nghị lane xương sống quyết một lần: hoặc đánh số theo thứ tự
đọc, hoặc chấp nhận số hiệu chỉ là nhãn. Hiện đang ở giữa hai đằng.

### 2. Luật "hai nhánh" — **bỏ hẳn, không sửa**

Sửa thành "mười hai nhánh" là chép một bản kiểm kê vào chỗ đáng ra phải chứa một bất biến. Bản
kiểm kê đó đã sai sẵn (mười bốn, xem phát hiện 2) và sẽ sai lại ở PR sau.

Câu luật ban đầu có việc của nó: nói rằng sửa một hoạt động là chuyện hẹp và rẻ. Điều đó nay không
còn đúng, nên câu này không còn gì để bảo vệ. Bất biến thật sự về cách sửa đã nằm ở 08.01 ("bấm
thẳng vào giá trị cần sửa") và 08.03 ("lưu ngay từng thay đổi") — hai luật đó vẫn phủ mọi nhánh
trong số mười bốn. Bỏ luật cũ, không cần luật thay thế.

Vế sau của câu — *"Sửa tên là thao tác phụ"* — cũng nên đi cùng: nay tên là ô đầu tiên con trỏ nhảy
vào khi tạo hàng (luật 10, và `qa-11`), tức nó là thao tác chính.

### 3. `SPEC.html` có mục riêng cho Ghi 02 — **có**

`docs/SPEC.html` tự khai đối tượng đọc là "người tiếp quản dự án mà chưa từng đọc dòng code nào".
Với người đó, hiện trạng là: màn phức tạp nhất của site được nhắc đúng một lần, trong một câu nói
rằng nó *không có phần trình bày*.

Cân đối theo chính số liệu của SPEC: Ghi 02 chiếm 2 trong 7 bảng (`hour_logs`, `activity_kinds`),
1 trong 11 endpoint nhưng với bốn sub-resource, và 3 trong 20 migration (0009, 0018, 0019).

Nhưng **đừng chép 39 luật vào SPEC** — SPEC mô tả hiện trạng, `logic.ts` mới là chỗ chứa ràng buộc.
Một mục ngắn là đủ, và nên gồm đúng bốn thứ mà thiếu chúng thì người đọc hiểu sai dữ liệu:

- một hoạt động có thể có nhiều **lần**, nối bằng `parent_id`;
- **`mins` của hàng cha không dùng được** — cộng nó là đếm hai lần (phát hiện 5 ở trên);
- nhóm `khác` là chuỗi trong `hour_logs`, **không có dòng** trong `activity_kinds` (nợ từ lượt 05,
  vẫn chưa vào SPEC);
- ngày mở sổ 20/08/2026 là hằng số, mọi tỉ lệ "x/y ngày" tính từ đó.

## Nhắc lại chưa xử lý

- lượt 05 → 08: `docs/spine/SO-BAN-GIAO.md` vẫn không có mục nào của lane QA. `qa-28` cũng tự nêu ở
  mục *Còn nợ*. Nay là ba lượt liên tiếp cùng một chỗ.
- lượt 06: ba chỗ `SPEC.html` tự mâu thuẫn — số luật, đường ghi `notes`, module trên Trang chủ.
  Chưa kiểm lại lượt này.
- lượt 07: `docs/superpowers/` — 107 ô việc chưa tick và chỉ thị "implement this plan task-by-task".
- lượt 04: hai file HTML cũ trong `docs/` vẫn chưa commit.

## Đề xuất câu chữ (không tự áp dụng)

- Bỏ luật 14 thứ hai, cả hai vế. Không thay bằng gì.
- Bốn nhóm mới theo bảng ở câu hỏi 1, đánh số từ 16.
- `SPEC.html`: một mục "Ghi 02 — nhật ký luyện tập" với bốn ý ở câu hỏi 3.
- `SPEC.html`: ô số liệu "17 migration" → 20, "75 quy tắc" → 82.

**Một đề xuất về cách làm, không về câu chữ.** Hai ô số liệu của SPEC vừa được sửa đúng hôm 22.08
đã lệch lại sau một ngày, và đây là lần thứ hai tôi báo cùng hai con số đó. Đếm file migration và
cộng `items.length` của `LOGIC` là hai phép đếm máy làm được. `tools/design-audit.mjs` (PR #27) đã
lập tiền lệ đúng cho chuyện này. Đề nghị lane xương sống cân nhắc một `tools/spec-numbers.mjs` in
ra các con số thật, để lần rà sau không tốn một mục báo cáo cho việc đếm file.

---

## Phụ lục · 2026-08-23, sau phản hồi của lane QA

*Ngoại lệ có chủ ý với nguyên tắc "không sửa file đã nộp": báo cáo này chưa được merge, nên chưa
ai bắt đầu đánh dấu xong trên nó. Chủ site yêu cầu merge một bản chuẩn thay vì merge rồi chữa.
Phần "Phát hiện" phía trên **không sửa một chữ** — phụ lục này chỉ đóng sổ và ghi thêm.*

Lane QA đã sửa `qa-28` và chủ site đã merge. `main` nay ở `ca21c88`.

### Đã đóng

- **Phát hiện 1 — khối chú thích "twenty-six weeks of them" mồ côi.** Đã sửa và tôi đã kiểm lại:
  khối ấy nay nằm đúng chỗ, ngay trên `export function heatmap`
  (`frontend/src/lib/hoursStats.ts`), và câu mở đầu viết "`weeks` columns of them" — một tham số
  thay cho một con số sẽ già đi lần nữa. Cách sửa này tốt hơn cách tôi đề xuất.
- **Phát hiện 2 — "mười hai nhánh" đếm thiếu.** Đã sửa, nhưng **không** thay bằng "mười bốn".
  Lane QA áp chính lý lẽ ở câu trả lời số 2 của tôi ngược lại vào phát hiện số 2 của tôi: mười bốn
  cũng là một bản kiểm kê, và bản kiểm kê nào rồi cũng sai lại ở PR sau. `qa-28` nay mô tả theo
  tầng, kèm câu cảnh báo đừng thay con số này bằng con số khác, và neo `onPatchSitting` /
  `onRemoveSitting` để ai cần đếm thì đếm từ code. **Đây là cách xử lý đúng hơn đề xuất của tôi** —
  tôi đã đề nghị sửa con số, đáng ra phải đề nghị bỏ con số, đúng như tôi đã tự lập luận ở câu
  hỏi 2 cách đó vài dòng.

### Vẫn mở, chuyển cho lane xương sống

Phát hiện 3 (hai ô số liệu `SPEC.html`), 4 (hai file `0017`), 5 (`mins` hàng cha) — lane QA xác
nhận cả ba đều ngoài lane họ.

Một chi tiết lane QA bổ sung cho phát hiện 4, tôi đã kiểm và đúng: hai file `0017` đụng **hai bảng
khác nhau** — `0017_pinned_and_optional_order.sql` chỉ `alter table public.posts`,
`0017_unclassified_lowercase.sql` chỉ `update hour_logs`. Nên hôm nay chưa hỏng gì. Rủi ro là ở
file thứ ba nào đó đụng cùng một bảng: lúc ấy thứ tự chạy không suy ra được từ tên nữa.

### Phát hiện mới, phát sinh khi kiểm lại bản sửa

- **[SAI]** Con số 26 tuần đã chết còn một chỗ thứ hai, sống sót vì phép `grep` quá hẹp
  · code: `frontend/src/content/hours.ts:143` — "How much history the statistics ask the server
    for: **twenty-six weeks**." Lane QA `grep` cụm đầy đủ "twenty-six weeks of them" nên ra 0;
    `grep` cụm ngắn "twenty-six weeks" thì vẫn còn một dòng ở file khác
  · sắc thái quan trọng: **hằng số thì không chết**. `STATS_DAYS = 182` (`hours.ts:157`) là cửa sổ
    *lấy dữ liệu về*, khác với bề rộng *vẽ ra* của heatmap; nó đang được dùng thật ở
    `frontend/src/lib/hoursStats.ts:165` và `frontend/src/data/useHours.ts:79`. Lấy về nhiều hơn vẽ
    ra là hợp lý
  · nhưng **lý do thì chết**: cả đoạn giải thích tại sao là 26 lại lập luận bằng hình học của lưới
    — "At twenty-six the square is 27px and the grid 188px", "at twelve columns a square … is 58px
    across and the grid stands 400px tall". Lưới ấy nay là 15 tuần, ô tối đa 24px (`qa-22`,
    `qa-23`). Một hằng số đang sống, được bảo vệ bằng một lập luận đã chết
  · độ chắc: đã xác minh
  · nếu ai đó sau này đọc đoạn này rồi chỉnh `STATS_DAYS` cho "khớp lưới", họ sẽ chỉnh cửa sổ lấy
    dữ liệu vì một lý do không còn liên quan tới nó

*(Bài học cho cả hai lane, và cho chính tôi: khi dọn một con số đã chết, `grep` con số chứ đừng
`grep` cả câu.)*
