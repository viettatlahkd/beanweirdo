# Rà tài liệu · lượt 04

ngày: 2026-08-22
file đã rà: `docs/SO-TUNG-ELEMENT.html` (và bộ công cụ sinh ra nó, `docs/doi-soat-tool/`)
commit đã rà: `06b0e93` (main)

> File này là bảng đối soát **từng thuộc tính CSS** giữa bản design v4 và code, do một script
> sinh ra. Nó chưa được commit (`git status` báo `??`), cùng tình trạng với `SPEC.html` và
> `DOI-SOAT-DESIGN-V4.html`.
>
> Lượt này có lợi thế mà hai lượt trước không có: bộ công cụ còn nằm trong `docs/doi-soat-tool/`
> (`extract.js`, `gen.js`, `report.json`, `verdicts.json`). Nên phần lớn các con số dưới đây
> được đối chiếu thẳng với dữ liệu gốc chứ không phải đếm bằng mắt.

## Đính chính hai chỗ tôi ghi sai ở lượt trước

Không sửa file đã nộp, nên ghi ở đây:

- **lượt 03** ghi tổng test là "262 + 124 = 386". Backend thật ra **137** test (`npm run test:api`,
  đo lại trên `06b0e93`), nên tổng đúng là **399**. Tôi đếm backend lúc `main` còn ở `6ceab14`
  rồi không đo lại. SPEC ghi 245 — vẫn sai, nhưng sai theo con số khác.
- **lượt 03** xếp mục "1.830 dòng markup" là `[SAI]` với lý do file có 4.176 dòng. Xếp vậy là
  quá tay: file có một khối `<script type="text/x-dc">` chạy từ dòng 2254 tới 4174, nên phần
  markup chỉ khoảng 2.250 dòng, và "1.830 dòng markup" có thể là một cách đếm khác của phần đó.
  1.830 vẫn không khớp con số nào tôi đo được (2.253 / 2.131 không rỗng / 2.122 có tag), nhưng
  nhãn đúng phải là **[MƠ HỒ] — không nói rõ đếm cái gì**, không phải [SAI].

## Phát hiện

- **[SAI]** Mọi số dòng "Ở đâu trong design" đều lệch **5 dòng** so với file design trong repo
  · tài liệu: `docs/SO-TUNG-ELEMENT.html` — cột *Ở đâu trong design*, và hướng dẫn đọc: "mở `Coffee Study Blog v4.dc.html` nhảy tới dòng ấy là thấy"
  · code: `frontend/design/prototype/Coffee Study Blog v4.dc.html`. Sáu mẫu, lệch đều +5:

    | tài liệu ghi | thực tế trong repo | phần tử |
    |---|---|---|
    | dòng 141 | 146 | `toggleStats` |
    | dòng 155 | 160 | `kindDraft` |
    | dòng 271 | 276 | `r.editAt` |
    | dòng 280 | 285 | `eName` |
    | dòng 290 | 295 | `background:#F1F4EF` |
    | dòng 295 | 300 | `r.durBg` |

  · độ chắc: đã xác minh
  · hệ quả cụ thể: nhảy tới dòng 290 như tài liệu bảo thì gặp `</sc-if>`, không phải phần tử đang nói tới. Cả bảng dùng được, nhưng phải cộng 5 — mà không chỗ nào nói điều đó.

- **[SAI]** Bảng không tái lập được: script đọc design từ một thư mục tạm của phiên khác, thư mục đó đã mất
  · tài liệu: `docs/SO-TUNG-ELEMENT.html` — cả trang dựa trên dữ liệu do `extract.js` sinh ra
  · code: `docs/doi-soat-tool/extract.js:4` — `fs.readFileSync(SP + '/design/Coffee Study Blog v4.dc.html')`, với `SP = /private/tmp/claude-501/…/b05d2dff-…/scratchpad` (dòng 2). Thư mục `scratchpad` còn, nhưng `design/` bên trong **không còn**
  · độ chắc: đã xác minh (đã thử đọc, báo `No such file or directory`)
  · đây là nguyên nhân gốc của mục trên: bản design mà script đọc thiếu 5 dòng đầu so với bản trong repo, và không ai đối chiếu lại được nữa. Chạy `extract.js` lúc này chỉ ra lỗi đọc file.

- **[SAI]** Con số "84 máy báo nhầm" gộp hai thứ khác hẳn nhau: 9 mục đã soi tay và 75 mục chỉ khớp chuỗi
  · tài liệu: `docs/SO-TUNG-ELEMENT.html` — ô số liệu đầu trang, và phần *Cách đọc bảng*: "**Không lệch** — máy báo nhầm, tôi đã kiểm và code đúng — **có ghi lý do**"
  · code: `docs/doi-soat-tool/verdicts.json` có **46** kết luận tay: 31 `real`, **10** `ok`, 5 `intent`. Đếm nhãn hiển thị trong HTML: 9 "Không lệch" (một verdict `ok` rơi vào mục `soft` nên hiện thành nhãn khác) + 75 "Nhiều khả năng ổn" = 84
  · code: `docs/doi-soat-tool/extract.js:110` — `else if (literals.has(v)) soft.push([p, v])`. Tức `soft` chỉ có nghĩa "chuỗi giá trị này có xuất hiện đâu đó trong file, nhưng không gắn với đúng thuộc tính đó". `docs/doi-soat-tool/gen.js:25` gán cho **mọi** dòng soft cùng một câu "Nhiều khả năng ổn — gần như chắc chắn ổn", không kiểm từng cái
  · độ chắc: đã xác minh
  · vì sao đáng báo: câu "tôi đã kiểm và code đúng" chỉ đúng với 9/84. 75 mục còn lại chưa ai mở ra nhìn — nhưng đứng chung một ô số liệu với 9 mục đã kiểm.

- **[THIẾU]** Phần *Cách đọc bảng* nói "Bốn kết luận", bảng dùng năm nhãn
  · tài liệu: `docs/SO-TUNG-ELEMENT.html` — phần *Cách đọc bảng* giải thích đúng bốn nhãn: LỆCH THẬT · Không lệch · Cố ý · Cần xác nhận
  · code: `docs/doi-soat-tool/gen.js:47` sinh **năm** nhãn — thêm `soft` → "Nhiều khả năng ổn". Đếm trong HTML: nhãn này xuất hiện **75 lần**, nhiều thứ hai sau "Cần xác nhận" (78)
  · độ chắc: đã xác minh
  · nhãn xuất hiện nhiều thứ hai trong tài liệu lại là nhãn duy nhất không được giải thích ở phần hướng dẫn đọc.

- **[MƠ HỒ]** "1585 thuộc tính đã soát" — khoảng 10% chưa ai soi
  · tài liệu: `docs/SO-TUNG-ELEMENT.html` — ô số liệu đầu trang
  · con số thật: 78 mục tài liệu tự nhận "chưa mở tay từng cái", cộng 75 mục chỉ khớp chuỗi (xem trên) = **153/1585**, tức 9,7% chưa qua mắt người
  · độ chắc: đã xác minh
  · tài liệu **có** nói rõ điều này trong phần *Cách đọc bảng*, nên đây không phải giấu giếm — chỉ là chữ "đã soát" ở ô số liệu nói to hơn phần đính chính bên dưới.

- **[SAI]** Danh sách "31 lệch thật" đã có mục lỗi thời — nay code khớp design từng chữ
  · tài liệu: `docs/SO-TUNG-ELEMENT.html`, mục *Bài — cards* — "`color: #C25C2E` · LỆCH THẬT · Màu chữ 'bỏ lọc ✕' đang khác thiết kế" (`docs/doi-soat-tool/verdicts.json`, khoá `Bài — cards|color: #C25C2E`)
  · design: `Coffee Study Blog v4.dc.html:1847` — `color:#8A8A7C` kèm `style-hover="color:#C25C2E"`
  · code: `packages/post-renderer/src/Cards.tsx:228` — `color: clearHover ? '#C25C2E' : '#8A8A7C'`. Khớp cả trạng thái thường lẫn trạng thái rê chuột
  · độ chắc: đã xác minh
  · đã sửa ở commit `52d7829` ("a flavour group keeps its own colour in the filter bar"), tức sau ngày soát 19.08

- **[SAI]** Một mục "lệch thật" khác ghi sai giá trị code hiện tại
  · tài liệu: mục *Bài — article* — "`opacity: 0.8` · Breadcrumb ở trang bài đang để **0.7** thay vì 0.8"
  · code: `frontend/src/screens/Article.tsx:75` — `<Breadcrumbs style={{ opacity: 0.75 }} …>`
  · độ chắc: đã xác minh
  · vẫn còn lệch so với design (0.75 ≠ 0.8), nhưng con số "0.7" trong tài liệu không còn đúng. Ai đọc rồi đi tìm `0.7` sẽ không thấy.

## Đã kiểm lại và vẫn còn nguyên (không phải phát hiện, ghi để khỏi kiểm lại)

- *Ghi 01 — Notes*: nhãn bộ lọc vẫn `fontSize: 9.5` / `letterSpacing: '.2em'` (`frontend/src/screens/Notes.tsx:23–24, 224–225`), design đòi 10.5px / .24em.
- *Bài — cards*: ô nhập tạo nhóm hương và nút "+ tag" vẫn chưa có — không tìm thấy `#3E7A4E` / `170px` / `#DAD7C7` trong `packages/post-renderer/src/Cards.tsx`. Trùng với `docs/spine/SO-BAN-GIAO.md` mục C5.
- *Bài — report*: sáu mục cột ghi chú / vạch kéo vẫn chưa dựng. Trùng với C6 và với lượt 03 mục 01.
- *Trang chủ*: `line-height 0.92` vs `0.90` vẫn còn (`frontend/src/screens/Landing.tsx:216`). Trùng lượt 03 mục 09.
- Đường dẫn file trong `report.json` vẫn trỏ đúng: cả 21 file còn tồn tại, kể cả ba renderer đã dời sang `packages/post-renderer/src/`.
- Số học nội bộ của tài liệu khớp: 1387 + 31 + 78 + 84 + 5 = 1585, và bằng đúng `ok 1387 + soft 75 + hard 123` trong `report.json`. Bảng tỉ lệ từng màn hình cũng khớp.

## Nhắc lại chưa xử lý

- lượt 01, cả bốn mục — chưa mục nào được đụng tới. PR #24 (`spine/docs-02-spec-refresh`) đang mở
  và sửa `docs/SPEC.html`; chưa rà nội dung PR đó.
- lượt 03, chín mục "chỗ lệch" của biên bản v4 — vẫn chưa mục nào đóng.
- lượt 03, mục ngoài phạm vi về `docs/spine/SO-BAN-GIAO.md` B1 ("SPEC ghi 67 luật", thực tế 64)
  — vẫn còn nguyên trong sổ.

## Đề xuất câu chữ (không tự áp dụng)

- Cột *Ở đâu trong design*: cộng 5 vào mọi số dòng, hoặc thêm một câu ở phần hướng dẫn đọc:
  "số dòng tính theo bản export gốc; bản trong `frontend/design/prototype/` nhiều hơn 5 dòng đầu".
- Phần hướng dẫn đọc: "Bốn kết luận" → "Năm kết luận", và thêm mục giải thích "Nhiều khả năng ổn"
  đúng như nó là: *giá trị có xuất hiện trong file nhưng không gắn đúng thuộc tính — máy đoán, chưa ai mở*.
- Ô số liệu: tách "84 máy báo nhầm" thành hai ô — "9 đã kiểm tay, code đúng" và "75 máy đoán là ổn".
- Ô số liệu: "1585 thuộc tính đã soát" → "1585 thuộc tính đã trích · 1432 đã qua mắt người".
- `extract.js:4`: trỏ vào `frontend/design/prototype/Coffee Study Blog v4.dc.html` trong repo thay vì
  thư mục tạm, để lần sau chạy lại được. Đây là sửa code, thuộc lane khác — chỉ nêu.
- Rà lại 31 mục "lệch thật" trên code hiện tại trước khi ai đó dùng danh sách này làm việc:
  ít nhất 1 mục đã tự khỏi và 1 mục ghi sai giá trị hiện tại.
