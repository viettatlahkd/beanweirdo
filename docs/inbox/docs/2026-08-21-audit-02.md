# Rà tài liệu · lượt 02

ngày: 2026-08-21
file đã rà: `frontend/src/content/logic.ts` (bộ quy tắc — 3 phần, 14 nhóm, 64 luật)
commit đã rà: `6ceab14` (main)

> Đếm lại cho khớp: `LOGIC` có đúng **64** luật, không phải 67. Con số 67 là kết quả
> `grep "{ s: '"` — nó đếm nhầm thêm 3 dòng của `SCOPE_KEY` (`logic.ts:45–49`).
> `docs/SPEC.html` ghi 64, và 64 mới là con số đúng.

## Phát hiện

- **[SAI]** Luật nhóm 12 dẫn chiếu sai số nhóm — nói "ngoại lệ của quy tắc 06", nhưng luật bị làm ngoại lệ nằm ở nhóm 07
  · tài liệu: `frontend/src/content/logic.ts:135` — nhóm 12 `[[cards]]`, luật 1: "mở được nhiều thẻ cùng lúc để đối chiếu — ngoại lệ của quy tắc 06"
  · tài liệu (đầu kia): nhóm 06 là "Câu trích đầu trang Ghi" (`logic.ts:92–97`), không liên quan. Luật "mỗi lần chỉ một mục mở" nằm ở nhóm 07, luật 3 (`logic.ts:101`)
  · code: `git log` cũng gọi nó là 07 — commit `a949b73` "fix: follow rules 07 and 08"
  · độ chắc: đã xác minh
  · ghi chú thêm: kể cả sau khi sửa số, câu này vẫn thừa — luật `logic.ts:101` đã tự giới hạn phạm vi ở `[[notes]]` và `[[archive]]`, nên `[[cards]]` vốn không nằm trong đó và không cần được miễn trừ.

- **[SAI]** Nhóm 05 nói "mọi module đã tạo và chưa ẩn: luôn hiện ở sidebar và [[landing]]" — code lọc bỏ module đặc biệt
  · tài liệu: `frontend/src/content/logic.ts:90`
  · code: `frontend/src/data/useModules.tsx:109–110` — `readingModules` lọc `m.kind !== 'special'`; được dùng ở `frontend/src/components/Sidebar.tsx:190`, `frontend/src/screens/Landing.tsx:202`, `frontend/src/screens/IndexScreen.tsx:366`. Ghi 01 và Ghi 02 là module thật (`backend/supabase/migrations/0012_special_modules.sql`) nhưng không bao giờ hiện ra như module
  · độ chắc: đã xác minh
  · lưu ý: `docs/SPEC.html` mục *Mô hình nội dung* có mô tả ngoại lệ này ("Đặc biệt — Ghi 01 và Ghi 02 … không nằm chung lưới với các module kiến thức"). Nên đây là bộ quy tắc lệch khỏi SPEC, không phải SPEC lệch khỏi code.

- **[SAI]** Nhóm 07 nói ở `[[notes]]` bấm một mục thì xổ ra tại chỗ, "không mở trang mới" — trên trang Ghi 01 có hai loại mục, và một loại mở trang mới
  · tài liệu: `frontend/src/content/logic.ts:99`
  · code: `frontend/src/screens/Notes.tsx:162` — note thì xổ tại chỗ (đúng luật). Nhưng `frontend/src/screens/Notes.tsx:504` gọi `openPost(nav, p)` cho các bài xếp dưới Ghi 01, và `frontend/src/lib/openPost.ts:15–17` chuyển thẳng sang màn hình Article
  · độ chắc: đã xác minh

- **[SAI]** Nhóm 03 nói "Trên mọi trang: đệm 56px hai bên" — hai màn hình nhật ký không theo
  · tài liệu: `frontend/src/content/logic.ts:70`
  · code: `frontend/src/screens/Notes.tsx:428` — `padding: '44px 72px 56px'` (hai bên 72px); `frontend/src/screens/Hours.tsx:233` — `padding: '44px 60px 110px'` (hai bên 60px). Mọi màn hình còn lại đúng 56px (`Archive.tsx:44`, `Cms.tsx:453`, `Landing.tsx:210`, `DesignSystem.tsx:77`, `Logic.tsx:22`, `Article.tsx:28`)
  · độ chắc: đã xác minh

- **[SAI]** Nhóm 01 nói "Trên mọi trang: nền mặc định là kem #FDFBF2" — Ghi 01 và Ghi 02 dùng một nền khác, có chủ đích
  · tài liệu: `frontend/src/content/logic.ts:54`
  · code: `frontend/src/design/tokens.ts:70–83` khai báo hẳn một bộ màu riêng `notesTheme` ("cold white editorial") với `bg: '#FCFCFA'`, `ink: '#12120F'`; dùng ở `frontend/src/screens/Notes.tsx:428` và `frontend/src/screens/Hours.tsx:231`
  · độ chắc: đã xác minh
  · vì sao xếp [SAI] chứ không phải [THIẾU]: đây không phải hành vi lọt lưới — nó có tên, có token, có comment giải thích. Luật đang nói sai về một quyết định đã chốt.

- **[THIẾU]** Sidebar tự lật sang nền tối trên đúng hai màn hình, chưa luật nào ghi
  · tài liệu: nhóm 05 (Điều hướng, `logic.ts:84–91`) có 6 luật về sidebar — không luật nào nhắc tới chuyện đổi nền
  · code: `frontend/src/components/Sidebar.tsx:194` — `const dark = nav.screen === 'notes' || nav.screen === 'hours'`; bảng màu ở `frontend/src/components/Sidebar.tsx:47–55` (`bg: '#102F35'`)
  · độ chắc: đã xác minh
  · va chạm kèm theo: `#102F35` là màu nhóm 01 luật 6 dành riêng cho nhóm Practice (`logic.ts:59`), nhưng ở đây nó được dùng cả khi đang đứng trên Ghi 01 — một mục thuộc nhóm Public.

- **[THIẾU]** Ctrl+Z cố tình không chạy khi con trỏ đang ở trong ô nhập liệu
  · tài liệu: nhóm 08 luật 6 (`logic.ts:109`) nói "Ở mọi thao tác ghi … Ctrl + Z hoàn tác thao tác gần nhất", không nêu ngoại lệ nào
  · code: `frontend/src/lib/useUndoStack.ts:59–63` — nếu `activeElement` là INPUT / TEXTAREA / contentEditable thì thoát sớm, nhường cho undo của trình duyệt
  · độ chắc: đã xác minh
  · lưu ý: `docs/SPEC.html` mục *Vài hành vi đáng biết* có ghi hành vi này. Bộ quy tắc thì chưa — mà chính SPEC nói bộ quy tắc phải đủ để "người đọc bộ quy tắc thấy được toàn bộ bức tranh mà không phải đọc code".

- **[THIẾU]** Ghi rồi mới xác nhận (optimistic write) là hành vi cố định của mọi thao tác sửa, chưa luật nào ghi
  · tài liệu: nhóm 08 (`logic.ts:103–111`) mô tả cách sửa và cách lưu, nhưng không nói màn hình cập nhật trước rồi mới gửi lên máy chủ, cũng không nói giá trị có thể "nhảy" lại khi máy chủ trả về khác
  · code: `frontend/src/data/useHours.ts`, `frontend/src/data/useNotes.ts` — cập nhật state trước, gọi API sau, và refetch khi lỗi
  · độ chắc: đã xác minh (SPEC có ghi hành vi này; bộ quy tắc thì không)

- **[THIẾU]** Ghi 01 xếp note ghim lên đầu, rồi mới tới mới nhất
  · tài liệu: nhóm 13 `[[notes]]` (`logic.ts:140–144`) có 3 luật, đều về hình thức lưới — không luật nào nói về thứ tự
  · code: `frontend/src/data/useNotes.ts:64–65` — `.order('pinned', {ascending:false}).order('d', {ascending:false})`; cột `pinned` đến từ `backend/supabase/migrations/0010_longform_and_memo_templates.sql`
  · độ chắc: đã xác minh

- **[THIẾU]** Ảnh đại diện của một bài rơi xuống lấy ảnh đầu tiên nằm trong thân bài khi bài không có ảnh bìa
  · tài liệu: nhóm 04 (`logic.ts:76–83`) đã chốt luật cho *tiêu đề* và *mô tả* trong danh mục ("khai báo đúng một chỗ", "sapo → mô tả"). Ảnh trong danh mục thì chưa có luật tương ứng
  · code: `frontend/src/lib/postThumb.ts:38–39` — `post.hero_image_url || findSrc(post.body)`, đào tối đa 4 tầng qua `fig` / `items` / `sections` / `blocks` / `cards`
  · độ chắc: đã xác minh
  · đây là loại luật đáng ghi nhất trong nhóm này: nó là một quyết định biên tập (bài không đặt bìa vẫn phải có mặt bằng hình), không phải chi tiết kỹ thuật.

- **[THIẾU]** Ở Ghi 02, Project luôn xếp trước Task
  · tài liệu: nhóm 14 `[[hours]]` (`logic.ts:145–150`) có 4 luật, không luật nào nói về thứ tự hai hệ tag
  · code: `frontend/src/screens/Hours.tsx:357–358` (comment "Project first, then Task — the same order the rows use")
  · độ chắc: đã xác minh

- **[MƠ HỒ]** Nhóm 05 luật 6 nói "đã tạo và **chưa ẩn**", nhưng dữ liệu không có cờ ẩn
  · tài liệu: `frontend/src/content/logic.ts:90`
  · code: `backend/supabase/migrations/` — bảng `modules` không có cột nào để ẩn; `frontend/src/data/useModules.tsx:7–34` cũng không có trường tương ứng
  · độ chắc: đã xác minh
  · `docs/SPEC.html` đã liệt kê việc này ở *Khoảng trống đã biết* ("Module chưa có cách ẩn"), nên không cần xử lý lại — chỉ ghi ở đây để hai tài liệu khỏi lệch nhau khi ai đó sửa một bên.

## Nhắc lại chưa xử lý

- không có (lượt đầu rà `logic.ts`)

## Đề xuất câu chữ (không tự áp dụng)

- `logic.ts:135`: "ngoại lệ của quy tắc 06" → "ngoại lệ của quy tắc 07", hoặc bỏ hẳn mệnh đề
  ngoại lệ vì luật 07 đã tự giới hạn ở `[[notes]]` và `[[archive]]`.
- `logic.ts:90`: thêm vế loại trừ, ví dụ "…luôn hiện ở sidebar và [[landing]] — trừ module đặc biệt
  (Ghi 01, Ghi 02), vốn đã có trang riêng nên chỉ nhận bài chứ không tự liệt kê."
- `logic.ts:99`: thêm vế phân biệt hai loại mục, ví dụ "…xổ ra tại chỗ. Riêng bài hoàn chỉnh xếp
  dưới [[notes]] thì mở sang trang bài, vì nó có template riêng."
- `logic.ts:54` và `logic.ts:70`: thêm ngoại lệ cho hai màn hình nhật ký, hoặc thêm một luật
  phạm vi Template cho Ghi 01 / Ghi 02 với nền `#FCFCFA`, mực `#12120F`, đệm hai bên 72px / 60px.
- Nhóm 05: đề xuất thêm một luật — "Với sidebar: trên hai trang nhật ký (Ghi 01, Ghi 02) nó lật
  sang nền xanh than, chữ sáng; mọi trang khác giữ nền kem."
- Nhóm 08: đề xuất thêm hai luật — (a) Ctrl+Z không can thiệp khi con trỏ đang trong ô nhập liệu;
  (b) mọi thao tác sửa vẽ kết quả lên màn hình trước rồi mới gửi đi, máy chủ trả về khác thì màn
  hình chỉnh lại theo máy chủ.
- Nhóm 13: đề xuất thêm một luật về thứ tự — ghim trước, rồi mới nhất trước.
- Nhóm 04: đề xuất thêm một luật về ảnh trong danh mục — không có ảnh bìa thì lấy ảnh đầu tiên
  trong thân bài; không có ảnh nào thì mới vẽ khối màu.
- Nhóm 14: đề xuất thêm một luật — Project xếp trước Task ở cả thanh tag lẫn dòng hoạt động.
