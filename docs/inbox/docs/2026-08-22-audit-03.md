# Rà tài liệu · lượt 03

ngày: 2026-08-22
file đã rà: `docs/DOI-SOAT-DESIGN-V4.html`
commit đã rà: `5d7104c` (main)

> File này là một **biên bản đối soát**, không phải spec. Nên câu hỏi khi rà nó không phải
> "nó mô tả hệ thống đúng chưa" mà "những gì nó nói còn đúng không". Nó ghi ngày soát
> 19.08.2026; từ đó `main` đã đi thêm **40 commit**, trong đó có 8 PR của lane xương sống.
>
> `docs/DOI-SOAT-DESIGN-V4.html` cũng **chưa được commit** (`git status` báo `??`) — cùng
> tình trạng với `SPEC.html`, xem lượt 01.

## Trạng thái 9 mục "chỗ lệch cần sửa" — sau 40 commit, chưa mục nào đóng

| mục | nội dung | trạng thái tại `5d7104c` |
|---|---|---|
| 01 | Report thiếu cột ghi chú | **còn nguyên** — `packages/post-renderer/src/Report.tsx` không có cột phải, không có vạch kéo, không có memo theo khối |
| 02 | Bảng Report chưa kéo được bề rộng cột | **còn nguyên** — `frontend/src/admin/screens/Editor.tsx:745,751` có `addColumn`/`addRow`, không có bất kỳ chỗ nào đặt bề rộng |
| 03 | CMS › Sơ đồ, dòng không bấm được | **còn nguyên** — `frontend/src/screens/Cms.tsx:496–535`, không `onClick`, không `cursor: pointer` |
| 04 | CMS › Sơ đồ, mất cột loại bài | **còn nguyên** — `frontend/src/screens/Cms.tsx:344` gộp thành một chuỗi `` `${displayNumber(i)} · ${p.en}` `` |
| 05 | CMS › Sơ đồ, thiếu nhóm cha Templates | **còn nguyên** nhưng tiền đề đã đổi — xem phát hiện bên dưới |
| 06 | Trang chủ đếm sai số bài | **còn nguyên** — `frontend/src/screens/Landing.tsx:257` vẫn là `{posts.length}`, trong khi lưới module ngay dưới dùng `landingModules` (`useModules.tsx:143`) |
| 07 | Nhãn "chưa có bài — chưa hiện trên trang" | **còn nguyên** — `frontend/src/screens/Cms.tsx:760` |
| 08 | Trang bài mất màu rê chuột | **còn nguyên** — `frontend/src/screens/Article.tsx:105–115` chỉ đặt `cursor`, không đổi màu |
| 09 | Chiều cao dòng tiêu đề 0.90 vs 0.92 | **còn nguyên** — `frontend/src/screens/Landing.tsx:216` |

Chín mục đều đã xác minh lại trên code hiện tại. Bảng này là phần đáng dùng nhất của lượt
rà — không phải để trách ai, mà vì `docs/spine/SO-BAN-GIAO.md` hiện chỉ chép lại **một**
trong chín mục (C6 = mục 01). Tám mục còn lại chưa có mặt ở sổ bàn giao, tức là đang nằm
ngoài cơ chế theo dõi duy nhất của dự án.

## Phát hiện

- **[SAI]** Ô số liệu "9 màn hình khớp" mâu thuẫn với chính bảng "Đã khớp" của tài liệu
  · tài liệu: `docs/DOI-SOAT-DESIGN-V4.html` — dải số liệu đầu trang ghi "14 màn hình đã soát · 9 chỗ lệch · 2 cần sửa gấp · 6 khác biệt cố ý · 9 màn hình khớp". Bảng *Đã khớp — không cần đụng* liệt kê **12 dòng**, trong đó "Trang module × 3" đếm là 3 → **14 màn hình khớp**, không phải 9
  · mâu thuẫn nội bộ nặng hơn: "Trang chủ" nằm trong bảng *Đã khớp* với mô tả "Nhãn trên, **tiêu đề**, 3 cột dẫn…", trong khi mục 06 và mục 09 đều là lỗi của Trang chủ, và mục 09 đúng là về **chiều cao dòng tiêu đề**
  · độ chắc: đã xác minh (đếm bằng parser trên HTML: 9 `.finding`, 2 `.pill.high`, 6 dòng bảng "khác biệt cố ý", 12 dòng bảng "đã khớp")
  · hệ quả: người đọc dải số liệu tưởng 5 màn hình có vấn đề, đọc bảng lại thấy 14 màn hình "không cần đụng". Hai con số không thể cùng đúng.

- **[SAI]** "Đọc hết 1.830 dòng markup của bản export Claude Design v4" — file v4 duy nhất trong repo có 4.176 dòng
  · tài liệu: `docs/DOI-SOAT-DESIGN-V4.html` — đoạn dẫn đầu trang
  · code: `frontend/design/prototype/Coffee Study Blog v4.dc.html` — 4.176 dòng, 331 KB
  · footer của tài liệu nói bản được soát là "bản v4 ở gốc file zip — không phải bản v4 nằm trong thư mục `uploads/`". Trong repo **không có** thư mục `uploads/` nào, và cũng chỉ có đúng một file v4. Nên con số 1.830 không đối chiếu được với bất cứ thứ gì người tiếp quản cầm trong tay
  · độ chắc: đã xác minh (phần đếm dòng); phần "bản nào được soát" là **chưa xác minh** — file zip nằm ngoài repo (`../bean weirdo design.zip`)

- **[SAI]** Dòng "System conventions — 14 nhóm, 59/59 luật của design, cộng 5 luật mới · Khớp" đã lỗi thời
  · tài liệu: `docs/DOI-SOAT-DESIGN-V4.html` — bảng *Đã khớp*, và câu trong *Kết luận ngắn*: "Bộ quy tắc còn đủ 59/59 luật của design, cộng thêm 5 luật bạn đặt sau"
  · code: `frontend/src/content/logic.ts` nay có **68 luật** (nhóm 09 tăng từ 6 lên 10 luật, commit `8f7b938`). 14 nhóm thì vẫn đúng
  · độ chắc: đã xác minh (đếm bằng cách import `LOGIC` và cộng `items.length` từng nhóm)
  · ghi chú: 59 + 5 = 64 — con số của tài liệu **đúng tại thời điểm 19.08**. Đây là lỗi thời, không phải sai từ đầu.

- **[SAI]** Ba dòng khác trong bảng "Đã khớp" cũng đã lỗi thời
  · tài liệu: `docs/DOI-SOAT-DESIGN-V4.html` — bảng *Đã khớp*
  · "Sidebar … chấm màu module, số bài, chế độ nền tối ở Ghi 01/02 · Khớp" — nay sidebar xếp module thường trước rồi mới tới module đặc biệt, và lọc theo `visibility` chứ không theo `kind` (`frontend/src/data/useModules.tsx:125,143`; migration `0015_module_visibility.sql`)
  · "Bài dạng thẻ … Khớp" — PR #2 đổi hẳn: bánh xe hương 14 nhóm là từ vựng cố định, thanh lọc xếp theo thứ tự bánh xe (`packages/post-renderer/src/Cards.tsx`, +160 dòng)
  · "Ghi 02 … Khớp" — `frontend/src/screens/Hours.tsx` đổi 377 dòng từ đó: nhiều đồng hồ cùng lúc, bấm giờ đo thời gian thật, tag sửa/xoá được, phiên xếp theo dự án
  · độ chắc: đã xác minh (qua `git diff 6ceab14..HEAD --stat` và đọc code hiện tại)
  · vì sao đáng báo: bảng này được viết ra để nói "không cần đụng". Một dòng "Khớp" đã lỗi thời nguy hiểm hơn một dòng "lệch" đã lỗi thời — nó bảo người ta đừng nhìn.

- **[SAI]** Mục 05 còn nguyên nhưng tiền đề đã bị chính tài liệu này phủ nhận ở bảng dưới
  · tài liệu: mục 05 yêu cầu gom các trang khuôn thành một dòng cha "Templates — khung bài mẫu", các khuôn là dòng con. Nhưng bảng *Khác biệt cố ý* của cùng tài liệu ghi: design có "3 trang khuôn cố định: Article, Field report, Info cards", còn bản đang chạy là "một trang Templates duy nhất, kho khuôn mở rộng được"
  · code: `frontend/src/content/navItems.ts:106–113` — chỉ còn một mục `templates`, không có dòng con nào để gom
  · độ chắc: đã xác minh
  · mục 05 tự nhận ra điều này ("Sau đợt làm lại template, dòng cha này nên trỏ về trang Templates mới") nhưng vẫn để nguyên mức "Nên sửa" và vẫn nằm trong ước lượng "~40 phút" ở bảng cuối. Việc thật còn lại là gì thì không nói rõ.

- **[SAI]** Bảng "Đề xuất thứ tự làm" nói "sửa lại 3 luật nhóm 11", nhóm 11 có 4 luật
  · tài liệu: `docs/DOI-SOAT-DESIGN-V4.html` — bảng *Đề xuất thứ tự làm*, lượt 1: "làm cho đủ cột ghi chú, hay sửa lại 3 luật nhóm 11 cho khớp thực tế"
  · code: `frontend/src/content/logic.ts` nhóm 11 `[[report]]` có 4 luật — ba luật về cột ghi chú/vạch kéo/hai chế độ (mục 01 của tài liệu), cộng luật "bảng thêm được cột và dòng, kéo được bề rộng cột" mà chính mục 02 nói là sai nửa vế
  · độ chắc: đã xác minh
  · `docs/spine/SO-BAN-GIAO.md` mục C6 đã ghi đúng là **4 luật** — nên chỗ cần sửa là tài liệu này, không phải sổ bàn giao

- **[THIẾU]** Biên bản không neo vào commit nào
  · tài liệu: `docs/DOI-SOAT-DESIGN-V4.html` — phần đóng dấu đầu trang chỉ có "Bản design v4 · 18.08.2026 10:55" và "Ngày soát 19.08.2026"
  · vì sao là thiếu sót thật: một biên bản đối soát chỉ có giá trị khi biết nó soi vào bản code nào. Không có sha, sau 40 commit không ai trả lời được "mục 06 đã sửa chưa" mà không đọc lại toàn bộ code — đúng việc tôi vừa phải làm ở bảng trên
  · độ chắc: đã xác minh

- **[MƠ HỒ]** Mục 06 dùng con số cụ thể của một lần chạy, nay đã khác
  · tài liệu: mục 06 — "Design tính 2 bài · Đang hiện 3 bài", "Người đọc bấm vào rồi đếm được 2 bài, trong khi nút vừa nói 3"
  · code: cơ chế vẫn sai y như tài liệu mô tả (`Landing.tsx:257` đếm mọi bài đã đăng), nhưng số 2/3 là số của cơ sở dữ liệu hôm 19.08 — `SPEC.html` ghi 19 bài. Người đọc thấy "3 bài" không khớp thực tế dễ tưởng lỗi đã hết
  · độ chắc: đã xác minh phần cơ chế; con số trong DB là **chưa xác minh** (không có quyền đọc DB thật)

## Nhắc lại chưa xử lý

Bốn phát hiện của lượt 01–02 đã kiểm lại trên `5d7104c`:

- **lượt 01, mục "16 màn hình"** — vẫn còn (đếm lại: 11 + 6 = 17).
- **lượt 01, mục "121 test frontend"** — **phần code đã xong**: `package.json` ở gốc nay khai báo
  workspaces + `vitest` + `jsdom` + `@testing-library`, và `npm test` chạy typecheck rồi 262 test,
  tất cả xanh. Phần tài liệu thì lệch xa hơn trước: SPEC ghi 245, thực tế 262 (frontend +
  post-renderer) cộng 124 (backend, chạy riêng bằng `npm run test:api`) = **386**.
- **lượt 01, mục `/api/site` + `/api/modules`** — vẫn còn (`site.ts:18`, `modules/index.ts:79`).
- **lượt 01, mục "bảng notes không còn đường ghi vào"** — vẫn còn (`notes.ts:129`,
  `PostsPanel.tsx:259`).
- **lượt 02, luật nhóm 12 dẫn chiếu "quy tắc 06"** — vẫn còn, nay ở `logic.ts:139`.
- **lượt 02, luật nhóm 05 về module** — `docs/spine/SO-BAN-GIAO.md` mục A3/A4/A5 đã nhận việc.
  Không nhắc lại.

Thêm một chỗ SPEC lệch phát sinh sau lượt 01, chưa ai ghi: **"14 migration"** — nay có **17**
(`0015_module_visibility`, `0016_drop_post_display_number`, `0017_unclassified_lowercase`).

## Ngoài phạm vi lượt này — một mục cần báo ngay

Không thuộc file đang rà, nhưng để sang lượt sau thì muộn, vì nó sẽ dẫn tới một lần sửa sai
vào `SPEC.html`:

- **[SAI]** `docs/spine/SO-BAN-GIAO.md` mục **B1** ghi: "Bộ quy tắc — Ghi **67 luật**, thực tế nay
  là 68". `SPEC.html` không hề ghi 67 — nó ghi **"64 quy tắc hệ thống"** ở dải số liệu và
  **"Sáu mươi tư quy tắc"** ở mục *Bộ quy tắc* (file chưa đổi từ 19.08 12:56)
  · tài liệu: `docs/spine/SO-BAN-GIAO.md` — mục B, dòng B1
  · code/tài liệu đối chiếu: `docs/SPEC.html`; `frontend/src/content/logic.ts` = 68 luật
  · độ chắc: đã xác minh
  · việc thật vẫn là 64 → 68, nhưng lý do ghi trong sổ là sai, và câu "Con số 67 ban đầu là tôi
    ghi sai" đang mô tả một lỗi không có trong SPEC

## Đề xuất câu chữ (không tự áp dụng)

- Dải số liệu: "9 màn hình khớp" → "9 màn hình không có mục nào cần sửa" và sửa lại cho khớp
  bảng, hoặc bỏ hẳn ô này vì bảng bên dưới đã nói đủ.
- Bảng *Đã khớp*: bỏ dòng "Trang chủ" (có mục 06 và 09), hoặc thu hẹp thành "Trang chủ — khối
  màu module và ba kiểu dàn ảnh · Khớp" để không phủ lên phần đang lỗi.
- Đoạn dẫn: bỏ con số "1.830 dòng", hoặc ghi rõ đường dẫn file đã đọc và số dòng của đúng file đó.
- Thêm vào phần đóng dấu đầu trang một dòng "Commit đã soát: `<sha>`".
- Bảng *Đề xuất thứ tự làm*, lượt 1: "3 luật nhóm 11" → "4 luật nhóm 11".
- Mục 05: ghi lại phần việc còn thật sau khi kho template được làm lại, hoặc hạ xuống "đã lỗi thời".
- Ba dòng "Khớp" đã lỗi thời (Sidebar · Bài dạng thẻ · Ghi 02): thêm ngày kiểm bên cạnh mỗi dòng,
  để lần sau biết dòng nào đã cũ mà không phải đọc lại code.
