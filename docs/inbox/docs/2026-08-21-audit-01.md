# Rà tài liệu · lượt 01

ngày: 2026-08-21
file đã rà: `docs/SPEC.html`
commit đã rà: `6ceab14` (main)

> Lưu ý về mốc: `docs/SPEC.html` hiện **chưa được commit** (`git status` báo `??`).
> Nên sha ở trên chỉ neo được phía code, không neo được phía tài liệu — bản SPEC
> tôi đọc là bản trên đĩa lúc 19.08.2026 12:56. Nếu ai đó sửa SPEC mà không commit,
> lượt rà này không truy lại được.

## Phát hiện

- **[SAI]** SPEC nói "16 màn hình", đếm được 17 — và chính bảng trong SPEC cũng liệt kê 17
  · tài liệu: `docs/SPEC.html` — ô số liệu đầu trang ("16 màn hình") và mục *Frontend* ("Mười sáu màn hình"). Bảng ngay bên dưới liệt kê: Công khai 5 (Landing · IndexScreen · ModuleScreen · Article · Notes) + Luyện tập 1 (Hours) + Quản trị 5 (Cms · Templates · Archive · Logic · DesignSystem) + Soạn bài 6 (Login · NewPostWizard · TemplateStep · MetadataStep · Editor · Preview) = 17
  · code: `frontend/src/screens/` — 11 component (bỏ file `.test.`); `frontend/src/admin/screens/` — 6 component. Tổng 17
  · độ chắc: đã xác minh

- **[SAI]** SPEC nói frontend có 121 test và "tất cả đang xanh" — thực tế 88 case, và không chạy được bằng bộ công cụ của chính repo
  · tài liệu: `docs/SPEC.html` — ô số liệu "245 test tự động", và mục *Môi trường & triển khai*: "121 bài cho giao diện, 124 bài cho backend, tất cả đang xanh"
  · code: `frontend/` có 18 file `*.test.ts(x)`, đếm được 88 lời gọi `it()`/`test()`. `frontend/package.json` không khai báo `vitest`, `jsdom`, `@testing-library/*`, và không có script `test`; `frontend/vite.config.ts` không có khối `test` (không set `environment: 'jsdom'`). Chạy thử bằng `npx vitest run` từ `frontend/`: 65/88 fail với `document is not defined` / `window is not defined`
  · backend thì khớp: `backend/vitest.config.ts` + `npm run test:api` → 124 test (123 pass, 1 skip)
  · độ chắc: đã xác minh
  · ghi chú: hai vế của phát hiện này nên xử lý tách nhau — con số 121 là lỗi tài liệu, còn việc frontend thiếu test runner là lỗi code. Tài liệu chỉ cần sửa sau khi bên code chốt.

- **[SAI]** Bảng endpoint nói `/api/site` và `/api/modules` chỉ cần vé khi ghi — code bắt vé cho cả GET
  · tài liệu: `docs/SPEC.html` — mục *Backend*, bảng endpoint, cột "Cần vé": `/api/site` = "ghi", `/api/modules` = "ghi"
  · code: `backend/api/site.ts:18` và `backend/api/modules/index.ts:79` — `requireAuth(req, res)` chạy trước khi phân nhánh `req.method`, nên GET không có vé cũng bị 401
  · độ chắc: đã xác minh

- **[SAI]** Mục "Khoảng trống đã biết" nói bảng `notes` "không còn đường ghi vào" — đường ghi vẫn còn và vẫn đang nối vào giao diện
  · tài liệu: `docs/SPEC.html` — mục *Khoảng trống đã biết* › Dữ liệu: "Bảng notes hiện rỗng và không còn đường ghi vào — nội dung Ghi 01 đã chuyển hết sang bài viết"
  · code: `backend/api/notes.ts:126–137` phục vụ POST / PATCH / DELETE; `frontend/src/admin/lib/apiClient.ts:325,334,343` gọi ba method đó; `frontend/src/admin/components/PostsPanel.tsx:155` lấy `add` / `patch` / `remove` từ `useNotes` và dựng UI sửa note ngay trong tab Nội dung
  · độ chắc: đã xác minh
  · ghi chú: câu "nội dung Ghi 01 đã chuyển hết sang bài viết" cũng chỉ đúng một nửa — `frontend/src/screens/Notes.tsx:359` vẫn đọc `useNotes()` song song với `usePublishedPosts()`, trang Ghi 01 vẽ cả hai nguồn.

- **[MƠ HỒ]** "tất cả đang xanh" đọc ra được là "không có test nào bị bỏ qua"
  · tài liệu: `docs/SPEC.html` — mục *Môi trường & triển khai*
  · code: `backend/api/integration.test.ts` — 1 test `skipped` trong 124
  · độ chắc: đã xác minh

- **[MƠ HỒ]** Đoạn "Trần 12 hàm" tự mâu thuẫn về mặt câu chữ
  · tài liệu: `docs/SPEC.html` — mục *Backend*, hộp cảnh báo: "Gói Vercel đang dùng cho phép tối đa 12 hàm serverless. Hiện đang ở 11. Thêm endpoint thứ 13 sẽ làm hỏng deploy"
  · code: `backend/api/` — 11 file handler, khớp con số 11
  · vấn đề: đọc theo nghĩa đen thì "thêm endpoint thứ 13" nghĩa là thêm hai cái nữa mới hỏng, nhưng câu đứng ngay sau "trần 12" khiến người đọc hiểu thành "thêm một cái nữa là hỏng". Người tiếp quản không biết mình còn 1 chỗ hay 2 chỗ
  · độ chắc: đã xác minh (phần đếm); phần diễn giải là nhận xét câu chữ

- **[MƠ HỒ]** SPEC nhắc `MERGE-NOTES.md` mà không cho đường dẫn
  · tài liệu: `docs/SPEC.html` — mục *Khoảng trống đã biết* › Tồn đọng: "Một mục việc trong MERGE-NOTES.md chưa được đụng tới lần nào"
  · code: file nằm ở `frontend/design/prototype/MERGE-NOTES.md`, không ở gốc repo và không ở `docs/`. Người đọc SPEC lần đầu sẽ tìm nhầm chỗ
  · độ chắc: đã xác minh

## Đã đối chiếu và thấy ĐÚNG (ghi lại để lượt sau không rà lại)

- 7 bảng · 14 migration · 11 endpoint · 5 kiểu trình bày · 64 quy tắc — khớp.
- Số cột từng bảng đều khớp khi cộng dồn 14 migration: modules 20, posts 22, templates 8,
  site_settings 3, hour_logs 9, activity_kinds 5, notes 13.
- Bảng "Vòng đời một bài viết" khớp từng dòng với `ALLOWED_FROM` ở `backend/lib/posts.ts:161–169`.
- Bảng phân quyền khách đọc/ghi khớp với policy + grant trong migration 0003 / 0006 / 0007 / 0008 / 0013 / 0014.
- Danh sách màn hình cho phép theo khu vực khớp `frontend/src/lib/area.ts:53–67`.
- 6 nguồn dữ liệu khớp `frontend/src/data/`.
- Khoảng trống "Chưa có giao diện tạo template" khớp: `backend/api/templates.ts` chỉ có GET và PATCH.

## Nhắc lại chưa xử lý

- không có (đây là lượt đầu; `docs/inbox/docs/` trước lượt này rỗng)

## Đề xuất câu chữ (không tự áp dụng)

- Ô số liệu: "16 màn hình" → "17 màn hình".
- Mục *Frontend*: "Mười sáu màn hình" → "Mười bảy màn hình".
- Bảng endpoint, cột "Cần vé": `/api/site` và `/api/modules` đổi "ghi" → "Có".
- Mục *Khoảng trống đã biết* › Dữ liệu, đề xuất thay bằng ý: "Bảng `notes` vẫn được đọc bởi
  trang Ghi 01 và vẫn ghi được qua `/api/notes` + tab Nội dung. Chưa quyết định gộp nó vào
  `posts` hay giữ làm hai nguồn song song."
- Ô số liệu test: để trống hoặc ghi "backend 124 · frontend chưa chạy được" cho tới khi bên code
  quyết định có bổ sung test runner cho frontend hay không.
